# DIMENSION DRIFTERS — BACKLOG / TASK BOARD
**The single task tracker.** Design canon lives in `DIMENSION_DRIFTERS_MASTER_SPEC.md` (the bible); this file tracks *work* against it. Master spec §24 points here. Keep both in sync — when an item lands, tick it here AND log the decision in the spec's §25.

**Conventions:** IDs are `PREFIX-n` (`DEC` decision, `CODE` engine/game, `ART` assets, `DOC` specs, `INFRA` tooling/CI). Status: **🔴 Open · 🟡 In progress · ✅ Done.** Most-actionable at top.
**Updated:** 2026-06-15 · matches master spec v0.44.

---

## 🔴 Needs Mike (decisions / reviews — these block work)
- **CODE-7** **Aim-while-moving bug** (open, v0.24): mouse aim freezes if the cursor was at REST when a WASD key is pressed (resumes once you stop) — GAME-specific, not OS. Candidate fix shipped: aim now reads a **capture-phase DOM pointer listener** instead of Phaser's `activePointer`. **Mike to confirm** via the `mouseMoves` counter in the debug HUD: if it ticks while holding WASD → fixed; if it freezes → events aren't reaching the renderer (deeper Electron input investigation needed).
- **DEC-4** 🟡 **Pick the canonical VFX *pathway*** for the ~40-mechanic system (§14) from the **VFX Lab** (`localhost:5191/vfx-lab.html`). *Greatsword image **picked + wired** (candidate-8, v0.34) — resolved by example: the **quake-erupt mechanic = Codex hero skin + engine overlays** (the layered/chunk-atlas direction) is now the working template. Remaining: confirm that as THE canonical approach for the rest of the 40 mechanics, or pick a different lab pathway before fleeting.*
- **DEC-1** Confirm the **§4 three-tier sync model** (still `[PROPOSED]`). POC implements Tier 1 only. → fully locks §4.
- **DEC-2** **Art style sign-off:** review the 30-asset style test in the art-review UI (`localhost:8190`). Is the Behemoth×Darkest-Dungeon look locked? Any global tweaks before fleeting canon?
- **DEC-3** **M0 roster decision:** promote any explore (`x-`) ideas into M0 canon, and/or confirm the 18 canon assets to fleet. Pick a **2H/L weapon size-anchor** (current weapon anchor = Cleaver, size M).

## 🟡 In progress
- **CODE-10** ✅ **Card redesign ported** (§9/§14, v0.44) — **scaling grades real** in the sim (`WeaponDef.scalingGrades` + `weaponDamageMult`); `ArenaScene.buildCard` rewritten to the full-bleed layout (baked rounded art texture + Phaser overlay: gradient panel, grip pill, **live charges badge**, name/subtitle, **live damage equation** base+bonus=total, **grade chips**, **drawn icon stat row** ↔/⏱/◎); card art regenerated **full-bleed portrait** (1024×1536) + copied to `public/cards/`. Client typechecks; mock-verified with the new art. **Remaining (polish, when convenient):** live-playtest the in-game render; color-code the equation parts (currently one gold Text); populate the badge with **durability** once that §10 layer exists; regen portrait art for the other 26 weapons when they're brought into the game (CODE-9).
- **INFRA-6** 🟡 **Weaponsmith — per-weapon authoring tool** (the §14 `WEAPON_AUTHORING` orchestrator, as an app: `node tools/weaponsmith/server.mjs` → `localhost:5050`). *Now covers the **whole arsenal**: the roster is sourced from the **art catalog** (every subject tagged `"weapon"` across `subjects.json` + `subjects.explore.json` = **30 weapons**, grouped by class), not just `weapons.ts`; gameplay stats merge in where coded. Per weapon: a candidate **hero-skin** gallery (its own sliced art, or a bespoke `vfx-*` skin) + a **tailored VFX SUITE** = the shared **~28-layer palette** (`vfx-layers.js`, all 4 classes) toggled to my **per-weapon PRESET**, with a live composed preview + per-layer param sliders → **save** (image/suite/notes to `assignments.json`); **reroll** still shells out to artkit. All 30 presets authored + verified rendering (v0.40). Remaining (v2/v3): grow the palette to the planned ~40 mechanics, **codegen `weapons.ts` + `subjects.json` from a weapon registry** (INFRA-2), stats/requirements/modifiers + affixes/rarity/scaling editing, card-art pass, and **port the layer renderer into the live game** (CODE-8).*
- **ART-1** Style-test pilot — 30 `x-` assets generated (5 candidates each), image-anchored. *Awaiting Mike's review (DEC-2).*
- **ART-2a** M0 **enemy fleet** ✅ generated (5 candidates each) — Boothill (hands-only ✓), Mote + Pricklepulp (pure-blob ✓), Ronin + Gatlin (full). Build variety confirmed. *Awaiting Mike's picks (DEC-3); then weapons + card art remain for ART-2.*

## 🟢 Next up (prioritized, unblocked)
- **CODE-1** **Finish step 6 — enemies** (§15): ~~chase-only~~ — **Boothill spitter ✅**, **enemy separation ✅**, **Pricklepulp zoner puddle ✅**, and the **generic Tough tier ✅** (scale/glow/HP/dmg/XP, ramps with run time) all done (v0.26–v0.32). Remaining: **bespoke Ronin/Gatlin tough behaviors** (combo-dash / scatter-burst beyond the generic buff), tough collision-radius scaling, and **spawn/damage tuning**.
- **CODE-5** 🟡 **Weapon framework** (build-order step 4, §10). *Done: data-driven `WeaponDef`/`WEAPONS` + weapon-driven server swing; held-in-hand upright swing animation; **dual-wield** (v0.24); **2-handed stance** (`twoHanded`, v0.25); **first behavior block — earthquake AoE+VFX** (`quake`, v0.25); Q-cycle. Remaining: fuller behavior-block library (§10/§15-style modules), charges/durability, the 1-held + 2-orbiting arsenal + card carousel (§9), full 10-weapon roster, parry signature (step 5).*
- **CODE-6** 🟡 **Run loop step 7** (§6/§16): **POC done (v0.30–v0.33)** — boss **OLD RUST** (timed/B-key spawn, boss HP bar, 2.7× scaled stand-in art), **extraction portal** + "salvage banked" screen, and **§11/§12 attribute leveling ✅** (5 attributes, +1 class/+1 req auto + 1 flex in a 5s invincible window, cap 30, squad-shared XP, derived stats). Remaining: **§8 signature-augment node every 5 levels** (the 9-card parry-augment pool — the next leveling layer), **player-count difficulty scaling** (§6: more toughs ×players + HP-sponge, vs the current time-ramp), **bespoke + multi-phase boss** (§16), **rez-or-dead death model** (§6, replace POC auto-respawn), real **salvage banking** (§13).
- **ART-2** **Fleet the M0 canon roster** (18 assets, image-anchored): 5 enemies → Drifter anchor, 9 weapons → Cleaver anchor; then **two-pass card art**. *After DEC-2/DEC-3.*
- **CODE-2** Build-order **steps 4–5 — weapon framework + parry** (SPEC-01): data schema, behavior-block library v1, arsenal/charges/durability loop, parry augments. *Code half unblocked; placeholder art ok.*
- **CODE-8** 🟡 **VFX on the real engine (Phaser/WebGL) — Phase 1 done, continuing** (§14, v0.40–v0.42). Mike chose "rebuild the smith preview on Phaser." **Done (v0.42):** `vfx-engine.js` — WebGL preview with additive blending + GPU particle emitters + a post-process **Glow** filter (Phaser 4 Filters), driven by the shared PRESET suite, integrated into `app.js` (persistent re-parented canvas + live updates + canvas-2D fallback), hero-texture race fixed. **Remaining:** (a) **true threshold bloom** (extend the Filters chain past the single Glow) + tuning; (b) port the long-tail particle layers (lob-arc, spin-trail, saw-sparks, barrel-spin, pellet/shell variants) — currently they log "not ported yet" and skip; (c) **mesh-ribbon** slash trails (vs the additive crescent); (d) normal-mapped 2D lighting (§28.16); (e) **extract the engine into a shared module the live ArenaScene also imports**, so saved suites play in-world (today only the greatsword's baked quake renders in-game). Reading `assignments.json` suites at runtime pairs with (e).
- **CODE-9** 🔴 **Backfill `WeaponDef` stats + full §10 tags for the 26 art-only weapons** (v0.40). The smith now lists all 30 weapons, but only 4 are gameplay-coded; the 26 `x-`/Wild-West extras have art (+ a VFX preset) but no `weapons.ts` stats and only minimal tags. On promotion to canon (DEC-3), give each its structured §10 taxonomy (grip/size/delivery/fireMode/element/class/family/range/scaling) + gameplay stats so they're wieldable. *(Lands with the weapon-registry codegen, INFRA-2.)*
- **DOC-1** **SPEC-01 Weapon Framework** (schema, behavior blocks, VFX-mechanic registry, rarity/affix/scaling/use-model). *(Consider folding into the master spec per the no-parallel-docs rule.)*

## 📋 Backlog (later / when prerequisites land)
- **CODE-3** Client-side **prediction + reconnect** (mine `colyseus/tutorial-phaser`); **StateView AoI** before the 10-player load test (§4 Tier 1).
- **CODE-4** Build-order **steps 7–10**: run loop (timer/boss/extraction), loot+economy stub, game-feel pass (Hades hit-stop/shake/damage numbers), M0 exit criterion.
- **INFRA-1** **Harvest normalization guard** (`scale-normalize` + `palette-quantize` to §28.2, §28.6) — currently only chroma-key + contact-sheet exist.
- **INFRA-2** **`subjects.json`-from-weapon-data** wiring (art + gameplay share one source — §28.10), lands with SPEC-01.
- **INFRA-3** **schema-validate in CI** once the weapon-definition JSON schema exists (§26 #3).
- **DOC-2** SPEC-02 (Run & Co-op), SPEC-03 (Content Plan), SPEC-04 (Netcode Detail) — or fold into master spec.
- **DOC-3** HUD layout deliverable (§20).
- **INFRA-4** 🟡 Wire promoted anchors into the tracked client. *(Pipeline built — `harvest-install.mjs` slices + copies to `packages/client/public/sprites/` + generates typed `manifest.ts`; Drifter + Critter/Mote/Pricklepulp/Boothill installed. Remaining: weapons, the rest of the roster, and INFRA-1 scale/palette normalization.)*
- **INFRA-5** **First git commit** — repo is initialized but has **no commits yet**. Commit the M0 foundation when Mike's ready (commit only on request).

## ✅ Done
- **CODE/ART** **Really-long-sword support + sprite aspect fix (v0.38–v0.39, §10/§28.4)** — **`displayLength` is the size dial, not the art box**; size-class length bands (S≈55…XL≈200–360); pre-size texture target scales to ≈2× displayLength (long blades stay crisp); demo **Driftblade** (Masamune-homage nodachi, displayLength 320, Codex art+card, sliced to 640px) — renders genuinely long. **Sprites now keep painted aspect ratio** (fixed the rig-scale-clobber that elongated toughs; uniform-scale rule locked §28.4). **Orbiting weapons deprecated** (§9). CDP-verified.
- **CODE/ART** **Weapon polish + card carousel + per-weapon VFX (v0.35, §9/§14)** — greatsword **slam targets the cursor** (clamped reach); **character renders over** the quake VFX; **bigger thrown cleaver**; **VFX for every weapon** (quake / bowie slash-arc / cleaver throw+impact); **§9 card carousel in-game** with **real two-pass Codex card art** (generated for all 3 weapons → `public/cards/`). CDP-verified. *(POC: card-art curation + frame/rarity border, bespoke bowie/cleaver hero skins deferred.)*
- **CODE** **Greatsword VFX wired + Cleaver thrown (v0.34, §9/§10/§14)** — the **Tombstone Greatsword** earthquake VFX authored in the Weaponsmith (candidate-8 + tuned params) is **baked into the weapon** and rendered in-game (closes the tool→game loop, replaces the procedural ring). The **Rusty Cleaver is now a thrown weapon**: RMB hurls spinning cleavers, **3 charges + refill cooldown** (§10), friendly piercing projectiles (STR-scaled, squad XP), HUD pip readout. CDP-verified (charges 3→0→refill; hero VFX erupts). *(POC: durability/break + repair, throw anim deferred.)*
- **CODE/DOC** **Parity-audit fixes (v0.33)** — audited the session vs the LOCKED spec; per Mike's rulings: **leveling reworked to the §11/§12 five-attribute model** (STR/DEX/INT/CON/LUK, +1 class/+1 req auto + 1 flex in a 5s invincible window, cap 30, squad-shared XP, derived stats; replaces the v0.31 card draft); **zoner puddle → unparryable + red/orange** (§8/§15); **extraction → "salvage banked"** not "victory" (§6). CDP-verified (auto-alloc, flex pick→derived maxHp, 5s auto-resolve, red puddle). *(Deferred per Mike: netcode tiers, player-count scaling, boss phases, rez model.)*
- **CODE** **Enemy variety — zoner puddles + tough tier (v0.32, §15)** — Pricklepulp drops lingering **corrosive puddles** (area-denial DoT, neon-acid render) as it moves; non-swarm spawns roll a **Tough tier** (ramps with run time) that scales up + glows + tanks/hits/rewards more. Both server-authoritative, CDP-verified (12 puddles, 5 tough concurrent @70s). *(POC: bespoke Ronin/Gatlin behaviors still TODO — CODE-1.)*
- **CODE** **Upgrade-card draft (v0.31, §12)** — level-up = **pick 1 of 3**; server queues a draft + freezes/invulns the chooser (co-op-safe), client shows a dim overlay of colour-bordered cards, click applies the stat. 6-card POC pool (Vitality/Power/Swift/Field Medic/Haste/Prospector) → real `PlayerState` stats in the sim. CDP-verified. *(POC: weapon-evolution/synergy + rarity/reroll deferred.)*
- **CODE** **Run loop — spitter + leveling + boss/extraction (v0.30)** — server-authoritative, CDP-verified: (1) Boothill **spitter** kites + fires dodgeable/parryable **projectiles** (neon spit); (2) **leveling** (§12) — XP on kill, geometric curve, +maxHp/+damage auto-buffs, XP bar + "LEVEL UP!" VFX; (3) **boss OLD RUST** (timed/B-key, HP bar, 2.7× scaled art) → **extraction portal** → **VICTORY**. The first complete M0 run loop. *(POC: upgrade-card draft + bespoke boss art deferred — see CODE-6.)*
- **INFRA** **Weaponsmith tool + VFX Lab (v0.29)** — see INFRA-6; the §14 WEAPON_AUTHORING orchestrator (VFX pick-and-choose MVP) + a 9-pathway VFX comparison lab.
- **ART/INFRA** **VFX ground-exclusion + HD→game-res pre-size (v0.27–v0.28)** — ground-effect VFX bake no floor (`vfxRenderRules`); `harvest-install` pre-sizes sprites to ~2× on-screen (Lanczos) + scales manifest geometry + runtime mipmaps/MSAA — fixed the "HD weapons look janky" minification aliasing.
- **CODE** **Playtest iteration (v0.21–v0.23)** — render-loop fix (`forceSetTimeOut`, kills the rAF-park "nothing spawns" class), Restart Run (R) + respawn breathing-room, difficulty tone-down, **weapon framework v0** (3 weapons held-in-hand + swing anim, Q-cycle, weapon-driven damage — see CODE-5), and a **Testing Grounds** (T): stationary dummies + walk-over weapon pickups (§21 prototype). All verified live.
- **CODE** **First Wild West level (POC, v0.20)** — playable survive-the-swarm slice: sliced-procedural sprite rig (`SpriteRig`, real Drifter/enemies, §18), Wild West arena (§28.7), enemies + ring-spawn director (§15, 4 archetypes chase-only), fists-placeholder melee + contact damage + always-on regen + death/downed/respawn (§6), damage numbers + white hit-flash + screen shake (§20). Build-order step 6 + slices of 4/9. Verified live. *(See §25 for the build-order divergence note + what's deferred.)*
- **INFRA** **Harvest-slice tool** (`tools/artkit/guards/slice.mjs`, was INFRA-6) — connected-component slicer cuts a keyed sprite into gap-separated parts (body/hands/feet or weapon pieces), auto-classifies build type; zero hand-cutting (§26 #2, §28.11). Plus `harvest-install.mjs` → typed sprite manifest for the client rig.
- **CODE** Build-order **steps 1–3**: monorepo scaffold + CI gates; netcode handshake POC (20Hz authoritative, multi-client Tier-1 sync, interpolation); player core (procedural limbless character §18, server-authoritative body collision §5, cursor aim §9). *(v0.4–v0.7)*
- **CODE** **Electron shell** (`packages/desktop`, §3) — fixes the browser rAF-throttle class of bug (`backgroundThrottling:false` + disabled occlusion). `pnpm dev:desktop`. *(v0.6, v0.7)*
- **ART** **artkit forked** into tracked `tools/artkit/`; **art bible consolidated into master spec §28** (standalone doc deleted — single source of truth). *(v0.8, v0.12)*
- **ART** **Art pipeline live**: DD `style.json` (Behemoth×Darkest-Dungeon anchor), **reference-image generation** (`styleRef`), `#00ff00` chroma-key guard, contact-sheets, side-profile/no-shadow/uniform-detail/weapon-flat-side-view rules. *(v0.9–v0.13)*
- **ART** **Golden anchors locked:** Drifter #3, Rusty Cleaver #1, Critter #2, OLD RUST #3. 30-asset style test generated. *(v0.13)*
- **INFRA** **Art-review web UI** (`tools/artkit/review/`) — browse/pick/edit/re-roll, auto-keying. *(v0.13)* **Upgraded** to object-explorer sidebar (status filters + category tree + sort), grouped grid, keyboard nav (`/`,`1–9`,`←→`,`R`,`Esc`), candidate lightbox, toasts, progress stat, persisted view, mobile-responsive.
- **DOC** Reference projects logged (§27.4); repo conventions (compiled `shared`, `.js` imports, pure-sim) adopted. *(v0.5)*
