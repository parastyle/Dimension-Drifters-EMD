# DIMENSION DRIFTERS — BACKLOG / TASK BOARD
**The single task tracker.** Design canon lives in `DIMENSION_DRIFTERS_MASTER_SPEC.md` (the bible); this file tracks *work* against it. Master spec §24 points here. Keep both in sync — when an item lands, tick it here AND log the decision in the spec's §25.

**Conventions:** IDs are `PREFIX-n` (`DEC` decision, `CODE` engine/game, `ART` assets, `DOC` specs, `INFRA` tooling/CI). Status: **🔴 Open · 🟡 In progress · ✅ Done.** Most-actionable at top.
**Updated:** 2026-06-15 · matches master spec v0.52.

---

## 🔴 Needs Mike (decisions / reviews — these block work)
- **CODE-7** **Aim-while-moving bug** (open, v0.24): mouse aim freezes if the cursor was at REST when a WASD key is pressed (resumes once you stop) — GAME-specific, not OS. Candidate fix shipped: aim now reads a **capture-phase DOM pointer listener** instead of Phaser's `activePointer`. **Mike to confirm** via the `mouseMoves` counter in the debug HUD: if it ticks while holding WASD → fixed; if it freezes → events aren't reaching the renderer (deeper Electron input investigation needed).
- **DEC-4** 🟡 **Pick the canonical VFX *pathway*** for the ~40-mechanic system (§14) from the **VFX Lab** (`localhost:5191/vfx-lab.html`). *Greatsword image **picked + wired** (candidate-8, v0.34) — resolved by example: the **quake-erupt mechanic = Codex hero skin + engine overlays** (the layered/chunk-atlas direction) is now the working template. Remaining: confirm that as THE canonical approach for the rest of the 40 mechanics, or pick a different lab pathway before fleeting.*
- **DEC-1** Confirm the **§4 three-tier sync model** (still `[PROPOSED]`). POC implements Tier 1 only. → fully locks §4.
- **DEC-2** **Art style sign-off:** review the 30-asset style test in the art-review UI (`localhost:8190`). Is the Behemoth×Darkest-Dungeon look locked? Any global tweaks before fleeting canon?
- **DEC-3** **M0 roster decision:** promote any explore (`x-`) ideas into M0 canon, and/or confirm the 18 canon assets to fleet. Pick a **2H/L weapon size-anchor** (current weapon anchor = Cleaver, size M).

## 🔬 Audit findings (v0.52) — prioritized
*From the full multi-agent audit (architecture / sim-perf / parity / proven-patterns), each finding adversarially verified. Architecture verdict: sound. These are the real follow-ups, ordered.*

**Fix-now tier — ✅ DONE (v0.52):** DEX HUD label; §20/§23.3 orbiting + telemetry spec self-contradictions; **AUDIT-1** (`selectChainTargets`/`clampQuakeEpicenter` → `@dd/shared`, de-duped client/server; first test suite → **53 tests green**); **AUDIT-2** (`displayLength` synced to smith values 200/125/65 + consistency test); **AUDIT-3** (`setPatchRate`, `dt` clamp, host-gating, aim normalize); **AUDIT-4** (VFX bake re-run; CI regenerate-diff deferred — bake reads gitignored art); **AUDIT-5** (§8 parry M0 ruling, §27.1 reconcile, stale `inMeleeArc` comment). **Bonus:** `pnpm lint` was pre-existing repo-wide RED → fixed + dead `vfx-engine-test.html` deleted; **full CI gate green for the first time.**

**Before fleeting / scale (gates the 10-player + horde load test — coupled, start the design now):**
- **AUDIT-6** **Spatial grid + entity pooling + Tier-2 soft-sync** as ONE workstream (add a tick-time budget metric first). Every proximity query is brute-force; masked only by `MAX_ENEMIES=80`. Keep the cap until this lands. Pairs with **CODE-3** (StateView AoI for Tier-1; trash → Tier-2, NOT StateView). *(L)*
- **AUDIT-7** **Fixed-timestep accumulator** (`while accum>=TICK_MS step()`) + swept/segment projectile collision. *(M)*
- **AUDIT-8** **Reconnect** (`allowReconnection` + client token) + **client prediction** (split self out of interpolate, run shared `stepPlayerMovement` locally, seq-numbered input). §4 [LOCKED], tracked CODE-3. *(M each)*
- **AUDIT-9** **Dimension-chain room-transition seam** (`transitionToNextDimension`, Reldens pattern) — decide one-re-seeded-room vs room-per-dimension BEFORE layering dimension content (it shapes the run loop + the reconnect-grace window). *(L)*

*Nice-to-have / scheduled debt: ArenaScene God-object extraction (**CODE-8**), director-as-encounter-mixer, `onDispose` lifecycle, CI Node-version pin, fists fallback wiring-or-defer, server-seeded arena.*

## 🟡 In progress
- **CODE-10** ✅ **Card redesign ported** (§9/§14, v0.44) — **scaling grades real** in the sim (`WeaponDef.scalingGrades` + `weaponDamageMult`); `ArenaScene.buildCard` rewritten to the full-bleed layout (baked rounded art texture + Phaser overlay: gradient panel, grip pill, **live charges badge**, name/subtitle, **live damage equation** base+bonus=total, **grade chips**, **drawn icon stat row** ↔/⏱/◎); card art regenerated **full-bleed portrait** (1024×1536) + copied to `public/cards/`. Client typechecks; mock-verified with the new art. **Remaining (polish, when convenient):** live-playtest the in-game render; color-code the equation parts (currently one gold Text); populate the badge with **durability** once that §10 layer exists; regen portrait art for the other 26 weapons when they're brought into the game (CODE-9).
- **INFRA-6** 🟡 **Weaponsmith — per-weapon authoring tool** (the §14 `WEAPON_AUTHORING` orchestrator, as an app: `node tools/weaponsmith/server.mjs` → `localhost:5050`). *Now covers the **whole arsenal**: the roster is sourced from the **art catalog** (every subject tagged `"weapon"` across `subjects.json` + `subjects.explore.json` = **30 weapons**, grouped by class), not just `weapons.ts`; gameplay stats merge in where coded. Per weapon: a candidate **hero-skin** gallery (its own sliced art, or a bespoke `vfx-*` skin) + a **tailored VFX SUITE** = the shared **~28-layer palette** (`vfx-layers.js`, all 4 classes) toggled to my **per-weapon PRESET**, with a live composed preview + per-layer param sliders → **save** (image/suite/notes to `assignments.json`); **reroll** still shells out to artkit. All 30 presets authored + verified rendering (v0.40). Remaining (v2/v3): grow the palette to the planned ~40 mechanics, **codegen `weapons.ts` + `subjects.json` from a weapon registry** (INFRA-2), stats/requirements/modifiers + affixes/rarity/scaling editing, card-art pass, and **port the layer renderer into the live game** (CODE-8).*
- **ART-1** Style-test pilot — 30 `x-` assets generated (5 candidates each), image-anchored. *Awaiting Mike's review (DEC-2).*
- **ART-2a** M0 **enemy fleet** ✅ generated (5 candidates each) — Boothill (hands-only ✓), Mote + Pricklepulp (pure-blob ✓), Ronin + Gatlin (full). Build variety confirmed. *Awaiting Mike's picks (DEC-3); then weapons + card art remain for ART-2.*

## 🟢 Next up (prioritized, unblocked)
- **CODE-22** ✅ **Tough melee-COMBO enemy — RONIN duelist** (Mike's queue, v0.63) — new `duelist` archetype + EnemyKind `melee` block (`approach/range/halfArc/damage/hits/windup/swingGap/recover`). Server `stepDuelists` runs idle→windup→swing×N→recover; `duelistSwing` arc-damages players via `inMeleeArc` + bumps `EnemyState.atkSeq` (client triggers the swing anim). Visibly **wields** `x-sword-neon-katana` on its rig; **35% drop** on death (`dropWeapon` → `maybeDropWeapon` at all 3 kill sites). `contactDamage 0` (attacks, no passive touch), `weight 0.7` (rare). POC art = boothill rig; bespoke ronin art via CODE-21. Gate green, game verified loading + joining clean.
- **CODE-23** ✅ **Fancy spinning weapon pickups + card-art flood fix** (Mike's queue, v0.63). (1) **Faux-3D pickups (§9/§21)** — Mike: "rotating 3D sword/gun/staff, very shiny." `syncPickups` now spins the weapon sprite on a `0→TAU` counter (`scaleX = baseScale·cos(t)` = coin-flip Y-rotation) with an additive **shine sweep** (`α = max(0,cos)⁵·0.75`) glinting at edge-on, a glow ellipse, ground halo, light beam, and bob — all rarity-tinted via `WEAPON_ACCENT`. (True polygonal 3D would clash with the flat art; faux-3D matches it.) (2) **Card-art 404 flood fixed (§28.5)** — preload queued `card-<id>.png` for every wired weapon but only 4 have art; Vite returns 200+HTML for missing `public/` files, so Phaser's loader spammed `console.error` (burying real errors). New `tools/artkit/gen-card-manifest.mjs` → `card-manifest.ts` (`CARD_ART_IDS` = PNGs that exist); preload loads only those, rest fall back to the sprite/icon card. Verified: post-fix exactly 4 card requests, zero 404s.
- **CODE-20** 🔴 **Promote weapon concepts → art + gameplay** — `data/weapon-concepts.json` (107 designed concepts, tagged §10). Pipe a chosen subset through the art pipeline (Codex → harvest-slice → manifest) + code `WeaponDef`s for the ones we want playable. Big batch; do in waves.
- **CODE-21** 🔴 **Promote character concepts → art** — `data/character-concepts.json` (50 concepts, Codex-ready `artBrief`s, same Drifter rig). Run through the art pipeline (Mike OK'd the Codex cost). Decide playable vs NPC/enemy per concept.
- **CODE-18** 🟢 **WYSIWYG damage + multi-source weapons** (§10/§14, rulings LOCKED v0.56). **Foundation DONE (v0.56):** per-source scaling — `damageMultFromGrades` / `sourceDamageMult`, optional `scalingGrades` on the `thrown`/`quake`/`chainLightning` blocks, `resolveSwing` computes `power` per source; **INT is now a live damage attribute**. **First weapon DONE (v0.57):** Wyrmtooth exploding magma — new `WeaponDef.scatter{count,spread,speed,range,damage,pierce,scalingGrades,explode{radius,damage,scalingGrades}}`; `ProjectileState.explodeR` (synced) so the client blast = the server hitbox; server `fireScatter` (bakes per-source scaling) + `detonate` AoE on death + `resolveSwing` hook; client `makeMagma` ember + `spawnExplosion` (ring to exact radius). Blade `{str C,dex C}`, magma + explosion `{int B}`. Cosmetic `magma-scatter` layer retired (real projectiles replace it). 62 tests, schema in sync, no crashes. **Remaining:** (a) **live playtest** the magma visual + balance (headless drive was throttle-blocked); (b) **re-bake** `weapon-vfx.generated.ts` cleanly once the `out/x-sword-bone/scatter` art scratch is regenerated (the magma-scatter-off + scatter-asset entry are hand-edited for now — AUDIT-4); (c) ✅ §9 card now shows every source's live equation + grades (v0.59); (d) reuse `scatter`/`explode` for other weapons as desired. Ruling reminders: dangerous-looking VFX get real hitboxes, pure flourish does NOT; size/AoE FIXED (§14) — only damage scales.
- **CODE-19** 🟡 **Card wheel + durability mechanic** (§9/§10/§11, from the v0.59 card pass). (1) **L-shaped card wheel** (Mike's design): tiny cards line the LEFT edge + the BOTTOM edge, cycling top-left → bottom-right, growing toward the focus; the held weapon card is BIG at the bottom-left elbow. Replaces the fanned carousel in `updateCarousel` (positioning only — the §5 card itself is built). (2) **Durability MECHANIC** — the card shows a static max (`WeaponDef.durability`); build depletion-on-use + break + repair (needs a balance pass: wear rate, what break does). (3) ✅ **Requirement ENFORCEMENT — RULED v0.67:** the damage penalty (`requirementPenalty`/`effectiveDamageMult`: unmet req −12%/pt, floor 25%, amber on card) is now CANONICAL — Mike chose it over the side-bag can't-wield gate (DEC-REQ closed). Both (1)/(2) are display-ready; the mechanics are the work.
- **CODE-24** 🟡 **Salvage bag + parts economy** (§13, from the v0.67 drop/salvage). The drop/salvage CONTROL is built (tap `R` drop → grabbable pickup + fists; hold `R` salvage → `PlayerState.salvaged` tally + fists; `fists` WeaponDef added). **Remaining = the economy:** a real salvage bag (what's in it), weapons→tiered parts by rarity (§13 Scrap…Hexmatter), the mid-run bag vs the extraction salvage, and the HUD for it. Currently `salvaged` is just a counter for hold-to-salvage feedback. Pairs with the broader §13 loot/Gems/pack-open stub (CODE-6/step-8).
- **CODE-1** 🟡 **Finish step 6 — enemies** (§15): ~~chase-only~~ — **Boothill spitter ✅**, **enemy separation ✅**, **Pricklepulp zoner puddle ✅**, the **generic Tough tier ✅** (scale/glow/HP/dmg/XP, ramps with run time, v0.26–v0.32), plus the two NAMED toughs: **Ronin ✅** (duelist 3-hit combo, wields+drops a sword, v0.63) and **Gatlin ✅** (5-pellet scatter spread, v0.65). Remaining: **tough collision-radius scaling**, **spawn-timing gating** for the named toughs (they currently random-spawn from t=0 via weight, not time-ramped), Ronin/Gatlin **bespoke art** (CODE-21), and **spawn/damage tuning**.
- **CODE-5** 🟡 **Weapon framework** (build-order step 4, §10). *Done: data-driven `WeaponDef`/`WEAPONS` + weapon-driven server swing; held-in-hand upright swing animation; **dual-wield** (v0.24); **2-handed stance** (`twoHanded`, v0.25); **first behavior block — earthquake AoE+VFX** (`quake`, v0.25); Q-cycle. Remaining: fuller behavior-block library (§10/§15-style modules), charges/durability, the 1-held + 2-orbiting arsenal + card carousel (§9), full 10-weapon roster, parry signature (step 5).*
- **CODE-6** 🟡 **Run loop step 7** (§6/§16): **POC done (v0.30–v0.33)** — boss **OLD RUST** (timed/B-key spawn, boss HP bar, 2.7× scaled stand-in art), **extraction portal** + "salvage banked" screen, and **§11/§12 attribute leveling ✅** (5 attributes, +1 class/+1 req auto + 1 flex in a 5s invincible window, cap 30, squad-shared XP, derived stats). Remaining: **§8 signature-augment node every 5 levels** (the 9-card parry-augment pool — the next leveling layer), **player-count difficulty scaling** (§6: more toughs ×players + HP-sponge, vs the current time-ramp), **bespoke + multi-phase boss** (§16), **rez-or-dead death model** (§6, replace POC auto-respawn), real **salvage banking** (§13).
- **ART-2** **Fleet the M0 canon roster** (18 assets, image-anchored): 5 enemies → Drifter anchor, 9 weapons → Cleaver anchor; then **two-pass card art**. *After DEC-2/DEC-3.*
- **CODE-2** Build-order **steps 4–5 — weapon framework + parry** (SPEC-01): data schema, behavior-block library v1, arsenal/charges/durability loop, parry augments. *Code half unblocked; placeholder art ok.*
- **CODE-8** 🟡 **VFX on the real engine (Phaser/WebGL) — Phases 1–2 done, polish remains** (§14, v0.40–v0.48). Mike chose "rebuild the smith preview on Phaser." **Done (v0.42):** `vfx-engine.js` WebGL preview (additive + GPU particles + Glow filter), integrated into `app.js`. **✅ UNIFIED (v0.48):** the layer renderer is now **ONE canonical module** — `packages/client/src/vfx/vfx-render.js` (+ `vfx-layers.js` registry) — loaded by **BOTH** the live game (ESM import) AND the smith (`vfx-engine.js` slimmed to a thin host, fetches it over HTTP). `build-weapon-vfx.mjs` bakes authored `assignments.json` suites → `weapon-vfx.generated.ts` (+ copies painted/scatter assets to `public/vfx/`); `VfxPlayer.ts` plays each weapon's suite **in-world on the real swing** (pooled surfaces, painted hero + engine layers + scatter, default slash fallback). Verified: smith + game both run the shared renderer, all VFX types render in-game, no leak. **Remaining (polish):** (a) ✅ **in-world bloom DONE** (v0.50 — per-object `addGlow` on the `VfxPlayer` root) + **fiery spark overhaul** (hot texture, fire color-ramp, velocity streaks, gravity, embers, 2.5× density); (b) port the long-tail particle layers (lob-arc, spin-trail, barrel-spin, pellet/shell variants) — still log "not ported yet"; (c) **mesh-ribbon** slash trails (vs the additive crescent); (d) normal-mapped 2D lighting (§28.16); (e) dual-wield double-arc + thrown-flight VFX; (f) hot-reload suites (re-run codegen is manual today).
- **CODE-9** 🟡 **Backfill `WeaponDef` stats + full §10 tags for the art-only weapons** (v0.40; swords done v0.47). The smith lists all 30 weapons. **All 10 SWORDS now gameplay-coded + wired into the Testing Grounds** (v0.47): the 7 explore swords (Buzzcutter, Drowned Anchor, Rattler Sabre, Reaper's Lid, Spike Driver [thrown], Voltedge, Wyrmtooth) got first-pass `WeaponDef`s (stats by archetype, tunable) + installed held-sprites (`harvest-install --kind=weapon` → manifest) + auto-spawned pickups (server reads `WEAPON_IDS`). Verified in-game: all 11 pickups spawn, textures load, equip works. **Remaining:** the same backfill for **guns / casters / launchers** (still art-only), and stat tuning. *(Lands fuller with the weapon-registry codegen, INFRA-2.)*
- **CODE-16** ✅ **Fixed per-weapon VFX size + ruling** (§14, v0.49) — Mike's ruling locked: attack/AoE/VFX size is FIXED per weapon, never scaled by level/stat/augment (audit confirmed nothing violates it; only damage scales). New authored `WeaponDef.vfxRadius` (px) + `VFX_RADIUS_DEFAULT = 74` (WYSIWYG-calibrated); a **VFX-size slider** in the smith → `assignments.json` → `build-weapon-vfx.mjs` bake → `VfxPlayer`. Severed the legacy `range×0.45` VFX coupling. Quake AoE stays on its own `quake.vfx.radius`. Verified end-to-end (API/codegen/game/smith), typecheck clean.
- **CODE-17** 🟢 **Forge "Abilities & Mechanics" section** (§10 behavior blocks) — **UI built v0.55 (NL-authoring model)**. Mike's call: instead of a structured-params registry + codegen, the forge captures GAMEPLAY intent in WORDS and Claude implements it. The smith now has a "③ Abilities & Mechanics" panel: an **editable, living `description`** ("what it does" — Claude keeps it current, user can edit) + a **"make it happen" notes box** (`author.mechanics`); Save flags `author.pending` (⏳ in the list) and the user pings Claude, who builds the behavior block (`weapons.ts` block + server `resolveSwing` + VFX) as a **reviewable diff**, then updates the description. Both persist to `assignments.json` (server `/api/author`). Also **removed the engine-layer checklist** from the UI (Mike: "I don't need the list of engine-generated VFX") — the cosmetic suite is now authored from the "② Engine VFX — your words" box; weapon-size + VFX-splash-size sliders kept. Boundary `[LOCKED v0.51]`: cosmetic suite → client bake; mechanic → weapons.ts. *First mechanic (chain-lightning on Voltedge) shipped hand-authored v0.51 — proves the behavior-block pattern. **Descoped (Mike, v0.55):** the structured `mechanics.ts` registry + `build-weapon-mechanics.mjs` codegen — revisit only if NL→hand-authored becomes a volume bottleneck.* **First request queued:** Wyrmtooth (`x-sword-bone`) "meteors explode on impact" (promote the cosmetic `magma-scatter` to a real projectile + AoE mechanic).
- **CODE-15** **Bespoke card art for the 7 explore swords** — they currently fall back to the installed weapon sprite on the §9 carousel card (legible, but not full-bleed portrait art). The §9 carousel now preloads card art ONLY for `CARD_ART_IDS` (the PNGs that exist), so missing card art no longer 404s — generate two-pass portrait card art (like the coded 4), drop it in `public/cards/`, then **rerun `node tools/artkit/gen-card-manifest.mjs`** to register it. (2026-06-15; manifest gate added v0.63.)
- **DOC-1** **SPEC-01 Weapon Framework** (schema, behavior blocks, VFX-mechanic registry, rarity/affix/scaling/use-model). *(Consider folding into the master spec per the no-parallel-docs rule.)*

## 📋 Backlog (later / when prerequisites land)
- **CODE-3** Client-side **prediction + reconnect** (mine `colyseus/tutorial-phaser`); **StateView AoI** before the 10-player load test (§4 Tier 1).
- **CODE-4** Build-order **steps 7–10**: run loop (timer/boss/extraction), loot+economy stub, game-feel pass (Hades hit-stop/shake/damage numbers), M0 exit criterion.
- **INFRA-1** **Harvest normalization guard** (`scale-normalize` + `palette-quantize` to §28.2, §28.6) — currently only chroma-key + contact-sheet exist.
- **INFRA-2** **`subjects.json`-from-weapon-data** wiring (art + gameplay share one source — §28.10), lands with SPEC-01.
- **INFRA-3** **schema-validate in CI** once the weapon-definition JSON schema exists (§26 #3).
- **DOC-2** SPEC-02 (Run & Co-op), SPEC-03 (Content Plan), SPEC-04 (Netcode Detail) — or fold into master spec.
- **DOC-3** HUD layout deliverable (§20).
- **INFRA-4** 🟡 Wire promoted anchors into the tracked client. *(Pipeline built — `harvest-install.mjs` slices + copies to `packages/client/public/sprites/` + generates typed `manifest.ts`; Drifter + Critter/Mote/Pricklepulp/Boothill installed. Remaining: weapons, the rest of the roster, and INFRA-1 scale/palette normalization.)*
- **INFRA-5** 🟡 **First git commit DONE** (`e178c1b`, 2026-06-15) — M0 foundation + the session committed; `out/` scratch + `.artkit-tmp/` gitignored. **Remaining: PUSH** — `gh` isn't authenticated and no remote is set; Mike runs `gh auth login` (then `gh repo create … --source=. --push`) or supplies a repo URL, then push.
- **CODE-11** 🔴 **Bespoke Codex VFX "hero" art per weapon** (§14, ruled 2026-06-15) — the weapon-sprite-as-hero is interim; generate a painted effect skin (slash/blast/spray) for each weapon as `vfx-*` subjects. Batch after the in-game VFX port (CODE-8). Pairs with CODE-9.
- **CODE-12** 🔴 **Attack-speed scaling stat source is OPEN** (ruled 2026-06-15) — DEX is now damage-only, so attack speed is flat weapon cooldown with no attribute driving it. Pick a source (a future AGI/SPD, or none) when balancing.
- **CODE-13** ✅ **Swing-phase engine VFX in the Weaponsmith attack scene** (v0.47) — `vfx-engine.js#frame` now renders swing-trigger layers (`slash-arc`/`blade-trail`/`edge-trail`/`twin-slash`/`thrust-streak`) ALONG the blade during the windup→chop (positioned at the hand, opening toward the enemy, timed to swing progress), with hit/cast/area layers still firing post-impact at the enemy. Extracted a `renderLayers(mode)` helper ("swing" / "impact" / "all"). So e.g. Voltedge's "electricity per swipe" now trails the blade. *Built; live-review the look in the forge.*
- **CODE-14** ✅ **Magma scatter-shot mechanic (Wyrmtooth / x-sword-bone)** (v0.47) — `tools/artkit/slice-scatter.mjs` dissects the painted cluster via connected-component labelling → an equal-cell spritesheet (8 balls). New `magma-scatter` layer (`vfx-layers.js`) + engine `setScatter` + a particle emitter that flings each ball as a distinct frame (forward cone + gravity + tumble, no tint). Server serves `/scatter/<vfx>/sheet.png` + exposes `weapon.scatter`; app feeds it to the combined engine and suppresses the static hero for scatter weapons. *Built; live-review the look in the forge.* **Generalizable**: any painted cluster can now become a scatter layer.

## ✅ Done
- **DEBT** **Source-of-truth cleanup (v0.46)** — audited for branching truth/dead V1 code. **One VFX renderer:** `vfx-layers.js` → pure layer REGISTRY (metadata), rendering ONLY in `vfx-engine.js`; deleted the dead canvas-2D draws (~300 lines), dead `PRESETS`, and unused `vfx-mechanics.js`. **One vfx-subject convention** (`vfx-<weaponId>`; dropped `SUBJECT_MAP`, renamed the 4 legacy dirs). **Card-art install step** (`tools/artkit/install-cards.mjs`) — no more manual `out/→public/cards` copy. **Scaling cleanup** — removed dead `deriveStats.power`/`STR_DMG_PER` (damage is per-weapon §10 grades). `assignments.json` normalized to the V2 schema. Decisions recorded (greatsword baked=compiled-output / authored=source; roster: subjects=record, weapons.ts=gameplay subset). Typechecks + Weaponsmith verified post-cleanup. *(Remaining structural split: the game's `ArenaScene.spawn*` VFX vs the engine — CODE-8.)*
- **CODE/ART** **Really-long-sword support + sprite aspect fix (v0.38–v0.39, §10/§28.4)** — **`displayLength` is the size dial, not the art box**; size-class length bands (S≈55…XL≈200–360); pre-size texture target scales to ≈2× displayLength (long blades stay crisp); demo **Driftblade** (Masamune-homage nodachi, displayLength 320, Codex art+card, sliced to 640px) — renders genuinely long. **Sprites now keep painted aspect ratio** (fixed the rig-scale-clobber that elongated toughs; uniform-scale rule locked §28.4). **Orbiting weapons deprecated** (§9). CDP-verified.
- **CODE/ART** **Weapon polish + card carousel + per-weapon VFX (v0.35, §9/§14)** — greatsword **slam targets the cursor** (clamped reach); **character renders over** the quake VFX; **bigger thrown cleaver**; **VFX for every weapon** (quake / bowie slash-arc / cleaver throw+impact); **§9 card carousel in-game** with **real two-pass Codex card art** (generated for all 3 weapons → `public/cards/`). CDP-verified. *(POC: card-art curation + frame/rarity border, bespoke bowie/cleaver hero skins deferred.)*
- **CODE** **Greatsword VFX wired + Cleaver thrown (v0.34, §9/§10/§14)** — the **Tombstone Greatsword** earthquake VFX authored in the Weaponsmith (candidate-8 + tuned params) is **baked into the weapon** and rendered in-game (closes the tool→game loop, replaces the procedural ring). The **Rusty Cleaver is now a thrown weapon**: RMB hurls spinning cleavers, **3 charges + refill cooldown** (§10), friendly piercing projectiles (STR-scaled, squad XP), HUD pip readout. CDP-verified (charges 3→0→refill; hero VFX erupts). *(POC: durability/break + repair, throw anim deferred.)*
- **CODE/DOC** **Parity-audit fixes (v0.33)** — audited the session vs the LOCKED spec; per Mike's rulings: **leveling reworked to the §11/§12 five-attribute model** (STR/DEX/INT/CON/LUK, +1 class/+1 req auto + 1 flex in a 5s invincible window, cap 30, squad-shared XP, derived stats; replaces the v0.31 card draft); **zoner puddle → unparryable + red/orange** (§8/§15); **extraction → "salvage banked"** not "victory" (§6). CDP-verified (auto-alloc, flex pick→derived maxHp, 5s auto-resolve, red puddle). *(Deferred per Mike: netcode tiers, player-count scaling, boss phases, rez model.)*
- **CODE** **Enemy variety — zoner puddles + tough tier (v0.32, §15)** — Pricklepulp drops lingering **corrosive puddles** (area-denial DoT, neon-acid render) as it moves; non-swarm spawns roll a **Tough tier** (ramps with run time) that scales up + glows + tanks/hits/rewards more. Both server-authoritative, CDP-verified (12 puddles, 5 tough concurrent @70s). *(POC: bespoke Ronin/Gatlin behaviors still TODO — CODE-1.)*
- **CODE** **Upgrade-card draft (v0.31, §12)** — level-up = **pick 1 of 3**; server queues a draft + freezes/invulns the chooser (co-op-safe), client shows a dim overlay of colour-bordered cards, click applies the stat. 6-card POC pool (Vitality/Power/Swift/Field Medic/Haste/Prospector) → real `PlayerState` stats in the sim. CDP-verified. *(POC: weapon-evolution/synergy + rarity/reroll deferred.)*
- **CODE** **Run loop — spitter + leveling + boss/extraction (v0.30)** — server-authoritative, CDP-verified: (1) Boothill **spitter** kites + fires dodgeable/parryable **projectiles** (neon spit); (2) **leveling** (§12) — XP on kill, geometric curve, +maxHp/+damage auto-buffs, XP bar + "LEVEL UP!" VFX; (3) **boss OLD RUST** (timed/B-key, HP bar, 2.7× scaled art) → **extraction portal** → **VICTORY**. The first complete M0 run loop. *(POC: upgrade-card draft + bespoke boss art deferred — see CODE-6.)*
- **INFRA** **Weaponsmith tool + VFX Lab (v0.29)** — see INFRA-6; the §14 WEAPON_AUTHORING orchestrator (VFX pick-and-choose MVP) + a 9-pathway VFX comparison lab.
- **ART/INFRA** **VFX ground-exclusion + HD→game-res pre-size (v0.27–v0.28)** — ground-effect VFX bake no floor (`vfxRenderRules`); `harvest-install` pre-sizes sprites to ~2× on-screen (Lanczos) + scales manifest geometry + runtime mipmaps/MSAA — fixed the "HD weapons look janky" minification aliasing.
- **CODE** **Playtest iteration (v0.21–v0.23)** — render-loop fix (`forceSetTimeOut`, kills the rAF-park "nothing spawns" class), Restart Run (R) + respawn breathing-room, difficulty tone-down, **weapon framework v0** (3 weapons held-in-hand + swing anim, Q-cycle, weapon-driven damage — see CODE-5), and a **Testing Grounds** (T): stationary dummies + walk-over weapon pickups (§21 prototype). All verified live.
- **CODE** **First Wild West level (POC, v0.20)** — playable survive-the-swarm slice: sliced-procedural sprite rig (`SpriteRig`, real Drifter/enemies, §18), Wild West arena (§28.7), enemies + ring-spawn director (§15, 4 archetypes chase-only), fists-placeholder melee + contact damage + always-on regen + death/downed/respawn (§6), damage numbers + white hit-flash + screen shake (§20). Build-order step 6 + slices of 4/9. Verified live. *(See §25 for the build-order divergence note + what's deferred.)*
- **INFRA** **Harvest-slice tool** (`tools/artkit/guards/slice.mjs`, was INFRA-6) — connected-component slicer cuts a keyed sprite into gap-separated parts (body/hands/feet or weapon pieces), auto-classifies build type; zero hand-cutting (§26 #2, §28.11). Plus `harvest-install.mjs` → typed sprite manifest for the client rig.
- **CODE** Build-order **steps 1–3**: monorepo scaffold + CI gates; netcode handshake POC (20Hz authoritative, multi-client Tier-1 sync, interpolation); player core (procedural limbless character §18, server-authoritative body collision §5, cursor aim §9). *(v0.4–v0.7)*
- **CODE** **Electron shell** (`packages/desktop`, §3) — fixes the browser rAF-throttle class of bug (`backgroundThrottling:false` + disabled occlusion). `pnpm dev:desktop`. *(v0.6, v0.7)*
- **ART** **artkit forked** into tracked `tools/artkit/`; **art bible consolidated into master spec §28** (standalone doc deleted — single source of truth). *(v0.8, v0.12)*
- **ART** **Art pipeline live**: DD `style.json` (Behemoth×Darkest-Dungeon anchor), **reference-image generation** (`styleRef`), `#00ff00` chroma-key guard, contact-sheets, side-profile/no-shadow/uniform-detail/weapon-flat-side-view rules. *(v0.9–v0.13)*
- **ART** **Golden anchors locked:** Drifter #3, Rusty Cleaver #1, Critter #2, OLD RUST #3. 30-asset style test generated. *(v0.13)*
- **INFRA** **Art-review web UI** (`tools/artkit/review/`) — browse/pick/edit/re-roll, auto-keying. *(v0.13)* **Upgraded** to object-explorer sidebar (status filters + category tree + sort), grouped grid, keyboard nav (`/`,`1–9`,`←→`,`R`,`Esc`), candidate lightbox, toasts, progress stat, persisted view, mobile-responsive.
- **DOC** Reference projects logged (§27.4); repo conventions (compiled `shared`, `.js` imports, pure-sim) adopted. *(v0.5)*
