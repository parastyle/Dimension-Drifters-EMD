# v8 Overcasters redo

## Understanding and plan

The reported failure occurs during sustained fire while moving, especially on later shots and hard strafe reversals. Projectile presentation separates from the rendered muzzle and the rendered character rig can separate from authority. The previous client-side recoil prediction attempt is explicitly out of bounds because it displaced the character and produced visible correction vibration.

This implementation will treat the defect as projectile presentation continuity: preserve each projectile's admitted rendered-muzzle origin and early visual flight, then converge smoothly onto its authoritative path without modifying predicted or authoritative character position. I will first inspect the original diagnosis, reverted commit `324a240`, and reporting instructions in the mandated order. I will then reproduce the honest moving-burst gate on private ports, trace projectile admission/update state, implement the smallest projectile-only correction, and extend the gate with during-fire and post-fire character-vibration bounds while keeping all existing thresholds unchanged.

Validation will retain before/after evidence, verify `rigAttackSeq` and authority sequence integrity, run the focused visual gate, and finish with the full `pnpm test` and `pnpm typecheck`. Any stack I start will use private ports and be torn down without touching the owner's live services on ports 5180/2567.

## Work log

- Read `v8-regressions-repeats.md`, revert `324a240` and its full message, then the reporting README in the required order. The honest diagnosis is narrower than the reverted first attempt: owner projectile admission already uses the live rendered muzzle, but the ordinary updater immediately attracts the visible container toward an authoritative row that has advanced multiple ticks. The reverted attempt mixed that valid projectile observation with client-side recoil prediction, pending impulse replay, recoil-specific reconciliation, and character presentation bounds; authority rejected those speculative impulses and the correction loop visibly vibrated during and after fire.
- The redo boundary is therefore explicit: no changes to `SelfPredictor`, no local recoil timing/impulse prediction, no character rig or position presentation clamps, and no authority movement changes. Only owner projectile presentation may carry admission continuity state. The permanent browser gate must separately prove both projectile continuity and stable character rendering during and after the trigger hold.
- Confirmed the checkout is on `feat/v0.118-metagame`; before implementation the only worktree change is this new report. The owner's ports remain untouched.
- Untouched focused baseline ran on the harness's private server port `62499` and failed the unchanged `18 px` projectile path assertion. It retained 224 frames, 38 rounds, and 10 bursts through all direction stages at `docs/owner-notes-audit-v8-evidence/a1-overcasters-redo/before/live-capture.json`. Admission remained exact (`0 px`), while following-frame path error reached `248.61 px`, authority-origin/muzzle separation reached `383.20 px`, and raw rendered-rig/authority separation reached `478.13 px`. The existing reverted-tree predictor diagnostic reported `0 px` only because its runtime fields no longer exist; the vibration extension must replace that vacuous metric with direct frame-to-frame rig motion and phase-labelled during/post-fire samples.
- Implemented the projectile-only continuity seam in `ArenaScene`: live gun-muzzle admissions skip the same-frame generic pull, integrate from the rendered muzzle at the row's authoritative velocity for a 400 ms opening-flight grace, then converge with an exponential correction capped at `500 px/s` until within `1 px` of the row. The state lives only on the projectile container; no player/predictor/recoil path was touched.
- First projectile-only live run used private port `54086` and retained 231 frames / 40 rounds / 10 bursts at `a1-overcasters-redo/projectile-only/live-capture.json`. Admission stayed `0 px` and maximum 350 ms path error fell from `248.61 px` to `1.64e-12 px`, comfortably below the unchanged `18 px` gate. The run then failed at the pre-existing `80 px` character/authority bound (`287.34 px`), confirming projectile continuity is fixed independently while the reverted tree's character diagnostic still needs an honest non-vibration definition and post-fire coverage.
- Audited the reverted tree's character path and found it still injected `SelfPredictor.addPredictedImpulse` on the trigger and every locally timed follow-up round. Removed those client-timed impulses to satisfy the hard constraint; server-authoritative recoil and cosmetic gun cues remain. A private-port comparison reduced maximum per-frame rig motion to `71 px` but allowed reconciliation separation to reach `609.27 px`, so it correctly remained red at the unchanged `80 px` authority bound. Rendering the raw predictor candidate directly was also tested and rejected: it still separated by `381.50 px` and would be an unrelated character-presentation change.
- Extended `burst-origin-moving.spec.ts` in the required redo evidence tree. It now releases fire after the sustained directional/reload sequence, keeps a real strafe held for a 1.5 second recovery window, labels every frame as during-fire or post-fire, and grades each phase separately. The existing `2.5 px` admission, `18 px` early path, `80 px` rig/authority, and `12 px` rig/predictor thresholds are unchanged. Added an `80 px` maximum per-frame rendered-rig step in both phases; this threshold fails the retained reverted-implementation captures (`96 px` and `121 px`) that the owner described as vibration. Added exact predictor-candidate telemetry plus monotonicity, maximum-one-sequence lead, and final authority-confirmation assertions for `rigAttackSeq`/`attackSeq`.
- The strengthened final-candidate run used private port `49779` and retained 273 frames / 40 rounds / 10 bursts at `a1-overcasters-redo/after-candidate/live-capture.json`. Projectile proof stayed green (`0 px` admission, `1.93e-12 px` early-path error). Authority and rig sequences were monotonic, never led by more than one, and ended confirmed at `10/10`. Post-fire rendered steps were stable at `15.47 px`, but the unchanged character constraints honestly failed: `444.09 px` maximum overall rig/authority separation, `172.87 px` post-fire separation, `271.63 px` maximum during-fire rig step, and `37.14 px` rig/predictor rejection.
- STOP decision required by the order: the projectile cause is fixed without touching player position, but the retained character gate cannot be made green on this tree without changing/clamping character prediction or presentation. Removing local recoil prediction (required) exposes the authority correction debt; rendering the predictor directly also failed; the reverted receipt/bound approach is forbidden and already owner-proven to vibrate. I will not relax the `80 px`/`12 px` thresholds or reinstate character displacement. The candidate remains deliberately non-shippable while the gate is red.

## Validation (last)

- Focused real-game gate: private port `49779`, 273 frames, 40 rounds, 10 bursts, all four keyboard directions and both hard reversals, followed by 1.5 seconds of post-fire strafing. Projectile checks passed (`0 <= 2.5 px` admission and `1.93e-12 <= 18 px` early path). Attack sequence integrity passed in the retained capture: zero authority regressions, zero rig regressions, maximum rig lead `1`, and final authority/rig equality `10/10`. Character acceptance failed honestly: overall separation `444.09 > 80 px`, post-fire separation `172.87 > 80 px`, during-fire step `271.63 > 80 px`, and rig/predictor delta `37.14 > 12 px`; only the post-fire step bound passed (`15.47 <= 80 px`).
- Full workspace health: `pnpm test` passed 133 files / 1,756 tests. `pnpm typecheck` passed shared, client, and server. Focused Biome formatting/checking passed with only the browser probe's pre-existing explicit-`any` warnings, and `git diff --check` passed.
- Line endings are verified byte-for-byte LF-only for `ArenaScene.ts`, `burst-origin-moving.spec.ts`, and this report (zero CRLF sequences). No generated output or catalog/assets changed, so generation and asset checks were not applicable. Every live run used the isolated harness on private ports (`62499`, `54086`, `57308`, `55256`, and `49779`) and tore down its own stack; ports `5180/2567` were never bound, stopped, or replaced.

VERDICT: NOT SHIPPED — the worktree contains a projectile-only muzzle-continuity candidate, removal of client-timed recoil impulses, an honest during/post-fire vibration gate, and retained before/after evidence, but character vibration is NOT provably absent because the unchanged character bounds remain red; exact gates are admission `<=2.5 px`, 350 ms projectile path `<=18 px`, rig/authority separation `<=80 px` overall and post-fire, rendered-rig step `<=80 px` during and post-fire, rig/predictor delta `<=12 px`, post-fire observation `>=1,200 ms`, at least 32 rounds / 8 bursts, zero authority/rig sequence regressions, rig sequence lead `<=1`, and final rig sequence equal to authority.
