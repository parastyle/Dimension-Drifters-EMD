# V7 Muzzle Verification

## Status

- 2026-07-21: Verification Sol started. Prep phase is active while waiting for `v7-muzzle-architecture.md` to announce the shared muzzle transform.
- 2026-07-22: **COMPLETE.** Shared architecture consumed; executable catalog is 138/138 clean in the definitive full sweep; contact sheet/evidence and sampled/full permanent gate are delivered; typecheck, Vitest, and complete e2e are green.
- Scope: catalog-wide stationary/strafing live probe, machine-readable results, annotated per-gun evidence, owner contact sheet, and sampled/full e2e gate.
- Constraints acknowledged: no git commit; do not stop the owner dev stack on ports 5180/2567.

## Work log

- Initialized this report before repository investigation, as required.
- Prep inventory initially found 139 active `gun || beam` schema candidates. Live dispatch audit refined that to **138 actual muzzle deliveries**: 116 projectile guns and 22 beams. The remaining Spore Spitter row is an explicitly reported channel-ground-zone exception whose legacy gun block never dispatches. The live set includes 6 authored multi-barrel weapons, 2 burst weapons, and 8 dual weapons.
- Existing coverage is not catalog-wide: `tools/gun-barrel-live-probe.mjs` accepts one projectile gun at a time, carries a six-id handwritten offset table, tests only a stationary shot, and does not cover beams/bursts/multiple lanes. `e2e/tests/gun-barrel-live-probe.spec.ts` invokes it for four rifles only.
- Prep design: one browser session will dev-equip the catalog sequentially, exercise stationary and right-strafe cases, record every authored muzzle lane/first burst, recover projectile origins (or read beam origins), and emit compact annotated evidence under `packages/client/public/muzzle-reference/` plus a single JSON sweep table.
- Prep implementation added: the live probe now derives membership from the catalog, has `--sample`/`--full`/explicit-id modes, records stationary and strafing cases in one browser session, freezes the exact first-shot coordinates in its annotation overlay while live state continues, groups pellet rows by recovered origin, follows burst born-ticks, and reads active beam origins. A generator now turns the full JSON/evidence set into `packages/client/public/muzzle-reference.html`.
- Permanent gate prep added in `e2e/tests/gun-barrel-live-probe.spec.ts`; the ordinary gate runs the behavior-stratified sample and `DD_FULL_MUZZLE_SWEEP=1` promotes it to all 138 executable muzzle deliveries while reporting the one non-muzzle schema exception.
- Sampling policy: every ordinary run includes all six base guns, all multi-barrel/burst/dual outliers, one deterministic representative of every `(gun|beam, family)` stratum, and a stable UTC-daily 1/24 rotating cohort. Full mode is required when catalog/muzzle geometry changes and for release qualification: `DD_FULL_MUZZLE_SWEEP=1 pnpm e2e -- gun-barrel-live-probe.spec.ts`.
- Prep syntax/format checks are clean. Playwright discovery is temporarily blocked by the architecture work-in-progress removing the old `CENTER_MUZZLE_OFFSET` export before the shared build/handoff is complete; no compatibility workaround was added because those old offset paths are explicitly being replaced.

## Architecture handoff

- **Received 2026-07-21 ET:** `v7-muzzle-architecture.md` announces “SHARED TRANSFORM LANDED — verification handoff ready.” Live verification phase started.
- Handoff API: `packages/shared/src/weapon-muzzle.ts` owns affine construction/composition and the sole `transformWeaponArtPoint` operation; `WeaponDef.muzzle` carries generated PNG-space points. Runtime server gun/beam paths and the live `SpriteRig` path have migrated.

## Validation and artifacts

- Clean machine-readable full table: `packages/client/public/muzzle-reference/sweep.json` (138/138 passed, two motion cases per row, 3 px gate, explicit non-muzzle exception metadata).
- Owner contact sheet: `packages/client/public/muzzle-reference.html` (138 cards, zero fail cards). Regenerate from the checked-in table/captures with `node tools/gen-muzzle-reference.mjs`.
- Annotated evidence: `packages/client/public/muzzle-reference/captures/` contains exactly 276 JPEGs: stationary and strafing for every executable delivery. Artifact audit found no missing paths/cases/shots, no authored-lane shortfalls, no incomplete bursts, and no moving case below 12 px travel. Riftglass's seven live fan rays legitimately share one authored emitter point.
- Visual QA: rendered the contact sheet locally and inspected its top grid plus Scattershell, Twin-Maw, Buzzard, and Stormcaller evidence. The page reports 138/138, zero fail cards, correct part-specific art markers, and legible cyan sprite-muzzle / green visible-origin overlays.
- `pnpm typecheck`: **passed** (shared build, shared/client/server no-emit checks).
- `npx vitest run`: **passed**, 128 files / 1,718 tests. The catalog PNG generator law now has an explicit 30-second test budget because full-suite worker load can exceed Vitest's 5-second default; its assertions are unchanged.
- Definitive full gate: **passed**, 1/1 Playwright test in 13.0 minutes; 138/138 rows clean.
- Complete `pnpm e2e`: **passed**, 20/20 tests in 10.8 minutes, including a fresh sampled gate at 48/48. An earlier contended pass had unrelated pistol-twirl/XP-echo timing flakes; both passed focused (3/3) and on this final complete rerun.
- Owner stack safety: no process on the owner's 5180/2567 stack was stopped or reused; live gates ran on private ephemeral Vite/Colyseus ports.
- No git commit was run.

## Failures returned to architecture

- **2026-07-21 ET — systematic transform failure, not yet an art override:** private-stack `x-gun-revolver-cannon` shakedown is 14.40 px stationary / 14.66 px strafing (3 px gate). The recovered server origin and authored Y agree closely (1.48 px stationary), but the live sprite muzzle is ~14.32 px farther forward. This indicates the server `WEAPON_GRIP_FORWARD` canonical pose and the final live rig hand/firing pose are not the same affine; changing the PNG muzzle point would move both and cannot cure this delta. Architecture Sol: please reconcile the server pose transform with the final live rig pose before override triage. Evidence table: `.tmp-bin/muzzle-e2e/sweep.json` (diagnostic, not final artifact).
- Cross-strata confirmation on the private current-code stack: Voltcaster beam 12.41/14.12 px, two-bore Hallowbore 14.68/14.93 px, and four-round Buzzard's Eye burst 23.10/26.10 px (stationary/strafing maxima). Every case fails forward; Hallowbore's authored two-point override cannot remove the common displacement, and burst deltas compound with recoil. The architecture needs a pose-affine correction before a 139-weapon sweep can become clean.
- Root cause narrowed to an exact pose formula: for the Revolver at aim-right, the live lead grip is `(pistol lead.x 0.22 + pistol aimReach 0.025 + self direct aim reach 0.10) * TARGET_BODY_H 76 = 26.22 px` forward of the rig root. The authority affine uses fixed `WEAPON_GRIP_FORWARD = 12 px`, predicting 14.22 px; measured forward error is 14.32 px. This is the family-specific hand-pose channel missing from the canonical transform, including its lateral component—not an alpha derivation or override-table problem. The exact formula was returned to the architecture report.
- Exact-transform telemetry rerun: stationary Revolver had `weaponLocal=(26.60, 1.48)`, rig root equal to shot-position X, and 14.66 px origin-to-painted-muzzle error, confirming the missing live grip anchor. While strafing, the live rig root was also 7.27 px ahead of the accepted authoritative player on the motion axis and total error was 15.35 px. The architecture handoff now distinguishes the common family-pose affine defect from this second prediction/authority root seam.
- Architecture correction consumed: family-specific lead/off-hand anchors moved into shared muzzle pose code and `SpriteRig` now mounts the real held PNG from that same pose; server gun rounds pass hand/recoil timing. A post-correction isolated Revolver run is clean: initial stationary authority-to-painted muzzle 0.0007 px and moving rendered-spawn-to-recorded-live-muzzle 0 px.
- Measurement policy was corrected for the prediction clock: the table retains recovered authoritative origin deltas for diagnosis, but grades the initial motionless shot against authority (the shared-affine proof) and every stationary/strafing round against the actual rendered admission origin plus its recorded live sprite muzzle. This matches the shipped local-prediction contract: simulation remains authoritative while new projectile art is visibly admitted at the final muzzle. Comparing a later predicted rig position to an earlier server fire tick produced false 4–290 px network/reconciliation failures and is not labeled a barrel transform failure.
- First 48-row sample exposed one real remaining geometry failure before cumulative player travel invalidated later rows: `x2-coyote-stinger` initial stationary authority misses the selected part-1/off-hand muzzle by 14.68 px laterally. `GameRoom.fireGun` supplies a logical `hand` to the grip pose but omits shared `salvoIndex`, so art-part selection follows global `attackSeq` while paired-hand selection can follow a rebased pair epoch. Filed to architecture as a dual-copy selection defect, not an art override. The private probe now resets Testing Grounds between rows so later trials cannot inherit recoil travel, pits, death, or projectiles; this reset is disabled for the owner's 5180 stack.
- Architecture's self-contained dual selection correction is now live-verified: isolated `x2-coyote-stinger` is 0 px at rendered admission and 0.00012 px on the initial authoritative affine check.
- The next clean 48-row sample found three expansion duals whose second PNG had not finished lazy-loading when `SpriteRig` was constructed. Their placeholder 32x32 part made the initial affine miss by 16.95-20.74 px even though the shared math selected the correct part. `ArenaScene.ensureWeaponArt` now declares a multi-part manifest ready only when **every** part texture exists and applies the same all-parts rule on loader completion. Isolated post-fix runs of Scattershell, Sidewinder, and Twin-Maw are all clean.
- A beam timeout on Riftglass Prism Lantern was traced to the probe cursor crossing the interactive right-side weapon dock, where beam input is intentionally suppressed. The probe now aims at a clear canvas point and Riftglass is clean in isolation (0 px rendered, 0.144 px initial authority).
- Sample gate run `.tmp-bin/muzzle-sampled-green/sweep.json`: all 48 rows are geometrically within tolerance; worst rendered-admission delta is floating-point zero (`4.55e-13` px), and worst initial authority check is 0.469 px. One Brinequill Tidescepter strafing trial timed out before firing after its stationary channel, so the overall run remains 47/48 rather than being mislabeled green. Harness reliability rerun is in progress before the public full sweep.
- Clean permanent-gate rerun `.tmp-bin/muzzle-e2e-sample-final2/sweep.json`: **48/48 passed**, 0 unexpected browser errors, worst rendered admission `4.55e-13` px, and worst initial authority check 0.115 px against the 3 px tolerance. The Brinequill timeout did not recur; it was a transient private-stack trial rather than a catalog or transform failure.
- First full public-artifact pass exercised all 139 schema candidates and recorded clean geometry everywhere it captured (worst rendered admission `4.55e-13` px; worst initial authority 0.225 px), but finished 135/139 because four trials were incomplete. Stormcaller passed a focused rerun. Buzzard's Eye Marksman and Galvanic Overcasters then exposed a probe flaw: pausing Phaser for the first screenshot let their 50 ms server burst follow-ups advance/disappear before the observer resumed. The overlay now freezes the exact recorded first-shot coordinates without pausing live state; both four-round weapons pass with screenshot capture enabled.
- Catalog membership correction: `x2-spore-spitter-blunderbuss` has a legacy `gun` stats block but its runtime `groundZone.trigger = "channel"` branch returns before gun dispatch and never creates a projectile/beam muzzle spawn. It is therefore explicitly listed in `excludedNonMuzzleRows` rather than passed or silently omitted. The executable muzzle-delivery catalog is **138** weapons (116 projectile guns + 22 beams); the contact-sheet header exposes the one excluded non-muzzle schema candidate and reason. No gameplay change was made to force a nonexistent gun shot.
- The private e2e probe now retries a thrown/interrupted live trial at most twice after a clean arena reset. Completed over-tolerance geometry is never retried, so this hardens long catalog runs without masking alignment failures.
- **Definitive full sweep green (2026-07-22 01:03 ET):** `packages/client/public/muzzle-reference/sweep.json` contains **138/138 passed** live gun/beam deliveries, each with stationary and strafing evidence. Worst rendered admission delta is floating-point zero (`2.27e-13` px); worst initial authoritative affine delta is 0.412 px against the 3 px tolerance; unexpected browser errors are 0. Brinequill Tidescepter, Carrion Roost Necro-Scepter, and Doomsday Drum Cannon each used one recorded transient retry (`attempts: 2`); every other row completed on its first live trial. The full Playwright gate passed in 13.0 minutes and regenerated `packages/client/public/muzzle-reference.html` plus both annotated captures per row.
