# V7 beam structures — durable report

## Understanding (first write)

- Role: Sol `v7-beam-structures`, Group 1. Work is concurrent with the hands and generated-identity-art Sols, so every write must stay inside this Sol's exclusive lease.
- Mission/ledger ownership: close systemic `V7-BEAM` by giving beams visibly different overall silhouettes—segmented arcs, converging strands, pulse trains, flame tongues, and related families—instead of changing only an internal waveform; also close the Frostquill Compendium Wave D row by rendering its beam entirely from ice-particle art.
- Source of truth: `docs/owner-notes-audit-2026-07-22-v7.md`, reconciled by `docs/sol-reports/plan-v7-remaining.md`.
- Exclusive writes: this report; `docs/owner-notes-audit-v7-evidence/beams/**`; `packages/client/src/vfx/BeamRenderer.ts`; `packages/client/src/vfx/BeamRenderer.test.ts`; `packages/client/src/vfx/caster-vfx-recipes.ts`; `packages/client/src/vfx/caster-vfx-recipes.test.ts`; new `packages/client/src/vfx/beam-structure-art.ts`; new `packages/client/public/vfx/beams/v7-structure/**`; new `tools/artkit/gen-v7-beam-structures.mjs`; and new `e2e/tests/v7-beam-structure-live-gate.spec.ts`.
- Read-only constraints: `packages/shared/src/hit-envelope.ts`, `packages/shared/src/weapon-muzzle.ts`, `packages/shared/src/weapons.ts`, `packages/server/src/rooms/GameRoom.ts`, `packages/client/src/entities/SpriteRig.ts`, all existing beam anchor/lifecycle gates, and every unlisted path. Authority width/range/origin and the shared muzzle affine are fixed inputs; no renderer change may widen reach or create client authority.
- Standing laws: damaging behavior remains server authoritative and DPS-neutral. Special-effect structure sheets will be Codex-generated bitmap images with retained prompts/outcomes/contact sheet. A thin continuous readable damage core or dense particle coverage must visually cover the full authoritative capsule, while energetic pixels remain inside its width and range.
- Permanent live gate: live-equip a representative of every structure family plus Frostquill; retain screenshots and renderer telemetry; measure rendered longitudinal occupancy/silhouette rather than recipe names; prove at least four non-equivalent signatures; prove Frostquill uses no non-ice core/body texture; and reassert moving muzzle attachment and beam lifecycle. No unit-only closure.
- Owner-stack safety: never stop, kill, replace, or seize the owner's listeners on ports 5180/2567. Any private listener must use the repository isolation idiom on non-owner ephemeral ports and clean up only processes started by this Sol. Browser/live activity must remain serialized.
- Primary risk: a renamed `widthProfile` could still look like one tube. Mitigation: compose generated structure art from the authoritative pose/width/range and gate measured rendered occupancy. Secondary risk: arc/particle overhang could lie about collision. Mitigation: fit all alpha/energetic pixels inside the authoritative capsule and test that bound.
- Work-order outline: inventory active beam recipes and current renderer/live behavior; choose a balanced, unmistakable family distribution; Codex-generate/install reusable transparent structure sheets; compose them data-first in `BeamRenderer`; make Frostquill ice-only through recipe data rather than a renderer weapon-ID branch; add renderer and recipe laws plus a multi-weapon permanent live gate; rerun existing anchor/lifecycle coverage; append final validation last.

## Work log

- Recorded the work order and constraints before any implementation write.
- Inventory: all 22 beam weapons already own distinct recipe signatures, palettes, width profiles, and internal ripple traces, but normal beams still paint four full-length nested capsules and two full-length particle ropes. Those broad continuous layers dominate the silhouette, so recipe distinctness can pass while live play still reads as one tube. The permanent fix must change the composed outer occupancy, not add another recipe-name assertion.
- Existing seams retained: `BeamRenderer.resolveDrawPose` already rebases the authoritative row to the final rendered muzzle; the server row remains the fixed width/range/origin input. `beam-anchor.spec.ts` measures live moving muzzle error at ≤2.5 px and `beam-lifecycle.spec.ts` drives the real accepted held-input lifecycle. Both will be rerun unchanged.
- Chosen reusable family representatives for before/after proof: Voltcaster Machine-Pistol → segmented arcs; Mirage Coilrifle → converging strands; Quartzlight Wayfinder → pulse train; Sunmote Reliquary Staff → flame tongues; Frostquill Compendium → ice particles. These active durable IDs avoid coupling this gate to the later Psalter archive.
- Added the new permanent gate in baseline-capable form. Its first run records the real authoritative active row, displayed rope texture, transformed rope origin, final weapon muzzle, and screenshots for all five representatives. The final form will extend those same live captures with texture-alpha occupancy signatures and capsule-bound checks.
- Baseline live result: the isolated gate passed its authority/muzzle prerequisites and retained five screenshots plus `before-live-capture.json`. All five transformed rope origins matched the final live muzzle exactly (maximum 0 px error), but the textures were ordinary full-length particle ropes (`ptcl:shock-bolt`, `ptcl:arcane-wisp`, `ptcl:holy-shard`, `ptcl:fire-mote`, `ptcl:frost-wisp`). Visual inspection confirms the broad nested capsules dominate every family; Frostquill is still a solid white/cyan tube rather than an ice-particle-only body.
- Asset plan: generate five reusable narrow horizontal sheets—segmented arcs, converging strands, pulse train, flame tongues, and ice particles. Each installs at 256×96 with at least a 7 px alpha inset. Runtime will scale that 96 px transverse canvas at or below authoritative width, so the source alpha plus retained rope endpoints cannot overhang the server capsule.
- Generated-art result: accepted five Codex-generated sheets at `packages/client/public/vfx/beams/v7-structure/`. The labeled contact sheet and machine-readable outcomes record exact prompts, source mode, 256×96 dimensions, alpha bounds, zero green-spill pixels, accepted candidate, and energetic longitudinal occupancy. Accepted occupancies are segmented arcs 0.7070, converging strands 0.9102, pulse train 0.6641, flame tongues 0.9219, and ice particles 0.8906. The artkit generator is resumable, preserves accepted survivors, keeps raw/log provenance under beam evidence, performs chroma removal plus post-resize despill, rejects alpha overhang, and requires energetic rather than single-antialias-pixel column mass.
- Recipe architecture: every one of the 22 beam definitions resolves through `BEAM_STRUCTURE_FAMILY_BY_WEAPON` to one of the five reusable families. This is presentation recipe data; `BeamRenderer` has no Frostquill or other weapon-ID structure branch. Per-family art width, thin readability-core width, and phase rate remain inset fractions of the authoritative beam diameter.
- Renderer architecture: the generated sheet bound to the retained rope supplies the outer alpha silhouette. Normal structured beams keep only a 4.5–7.5% thin continuous inset core so the full damage capsule remains readable through deliberate segment gaps. Frostquill's ice-particle recipe sets `iceOnly: true` and the procedural core to exactly zero; both visible retained ropes use only `beam-v7-structure:ice-particles`. Existing server-owned cone streams keep their truthful widening polygon and do not receive a constant-width rope that would overhang the narrow source end.
- Authority/balance: no shared/server definition, damage, cadence, heat, width, range, origin, or muzzle affine changed. DPS and authoritative hit geometry are byte-for-byte inputs to presentation. Generated alpha is measured after the actual live texture is bound; rope endpoints stay within rendered authoritative length, and transverse alpha extent plus animated normal wobble stays within half authoritative width.
- Permanent live gate: upgraded the baseline-capable gate to measure the alpha pixels from each texture actually bound to a visible live rope. It produces 32×12 occupancy grids, requires all ten family pairs to differ by at least 0.1, transforms actual alpha bounds through live rope scale/wobble, checks width/range containment, records screenshots and telemetry, and proves Frostquill has no non-ice body/lip texture or procedural core. A separate screenshot-free accepted cycle samples moving server position and rope-to-rendered-muzzle error; the unchanged dedicated beam-anchor gate retains its stricter >20 px rendered-root displacement law.
- Final live result before aggregate validation: all nine catalog assertions are true. Pairwise measured grid distances span 0.1276–0.2682; stationary muzzle error is 0 px for all five representatives; moving maximum error is approximately `4.55e-13` px across a synchronized 11.2 px server span; active→cooling lifecycle clears; and every energetic alpha envelope remains inside authoritative width/range. Human review confirms retained active frames visibly read as broken red segments, violet converging strands, discrete gold pulses, pointed flame tongues, and Frostquill's particle-only ice stream.

## Validation (final write)

- Generated-art install/validation: `node tools/artkit/gen-v7-beam-structures.mjs --install-only` passed. All five accepted Codex-generated sheets report `VALID_EXISTING`, exact `256×96` RGBA output, zero validation failures, energetic column occupancies of 0.707 / 0.910 / 0.664 / 0.922 / 0.891, retained prompts/outcomes/raw provenance, and production copies under `packages/client/public/vfx/beams/v7-structure/`.
- Static hygiene: targeted `pnpm exec biome check` passed for the leased implementation, tests, live gate, and generator (`Checked 5 files ... No fixes applied`). `git diff --check` exited 0; its only messages were line-ending warnings for already modified shared-worktree files.
- Type safety: `pnpm typecheck` passed across shared, server, and client.
- Unit/integration suite: the first aggregate run reached 1,728 passing tests and exposed one unrelated order-sensitive hostile-projectile-ceiling failure in `packages/server/src/rooms/GameRoom.test.ts:2409`. That exact test passed immediately in isolation. A clean aggregate rerun then passed all **128 files / 1,729 tests**. The beam renderer/recipe laws are included: all 22 active beam recipes resolve to the five structure families; generated alpha stays within server width/range; at least four measured silhouettes remain non-equivalent; and Frostquill resolves to ice-only art with zero procedural core.
- Permanent live gate and unchanged regressions: `pnpm e2e -- e2e/tests/v7-beam-structure-live-gate.spec.ts e2e/tests/beam-anchor.spec.ts e2e/tests/beam-lifecycle.spec.ts --workers=1` passed **3/3** in 1.0 minute. The repository harness used only private listeners (`ws://localhost:55535`, gate client `http://127.0.0.1:55537`) and closed its own stack; owner ports 5180/2567 were never stopped, replaced, or used.
- Final measured live evidence: all nine assertions in `docs/owner-notes-audit-v7-evidence/beams/after-live-capture.json` are true. All ten cross-family 32×12 alpha-signature distances exceed the 0.1 threshold (minimum 0.127604, maximum 0.268229); every representative binds its generated sheet; actual energetic alpha is inside authoritative transverse width and longitudinal range; all five stationary muzzle deltas are 0 px; the moving accepted-fire cycle covers 22.3999 px of synchronized server motion with 0 px maximum rope-to-rendered-muzzle error; phases 1→2→3 occur and the row clears; Frostquill's visible body and lip both bind `beam-v7-structure:ice-particles`, `iceOnly` is true, and procedural core width is 0.
- Authority/balance/scope audit: no server/shared damage, cadence, heat, range, width, origin, or muzzle source was edited. The change is presentation-only and DPS-neutral. The final worktree audit showed all of this Sol's writes inside its exclusive beam lease; concurrent hands/generated-art Sol paths were not touched. No git commit was created.
