# V7 movement — tumble roll and immediate long jump

## Understanding (first write)

- Baseline: `feat/v0.118-metagame` at Group 1 barrier commit `bd07261e3825f55a24ad980c595a5d9a2a267d02`; the worktree was clean before this report was created.
- Mission / ledger row: own `V7-MOVE` only. Replace the current Megabonk slide-hop with one quick, server-authoritative, fixed-distance dodge roll on cooldown whose rendered player card visibly tumbles. Make the existing distance jump the default Space jump and start it from a one-frame tap with no crouch, charge, or 650 ms hold/commit delay.
- Authority contract: movement timing and displacement must come from shared deterministic math used by `GameRoom` and prediction. Preserve the current defensive boundary: only contact, projectile, and locked-melee opening dodge rules; no AoE immunity or parry rewards. Preserve pit, pound, freeze, landing, and reconciliation behavior. Colyseus schema fields remain append-only and are not reordered.
- Presentation contract: input, authority, prediction, arena input/effects, and `SpriteRig` presentation move together. Weapon/catalog/VFX recipe surfaces, muzzle affine/grip surfaces, firing stance, secondary grip, and the HANDS implementation/report/gate are read-only.
- Permanent live gate: use real Shift/Ctrl and Space inputs on a private repository-isolated stack, never altering the owner's listeners on 5180/2567. Measure unobstructed roll distance across cardinal and diagonal directions, quick duration, cooldown rejection/acceptance, readable rendered tumble completion, and client/server reconciliation. Measure a one-frame Space tap entering distance-jump flight without `STANCE_CROUCH` or the old 650 ms delay, reaching the authored long-jump band while retaining pit/pound/freeze/landing behavior. Assertions must inspect measured motion and rendered geometry, not recipe metadata.
- Exclusive writes are limited to this report; `docs/owner-notes-audit-v7-evidence/movement/**`; shared `constants.ts`, `movement.ts`, and `state.ts`; server `GameRoom.ts` and `GameRoom.test.ts`; client prediction and tests, `ArenaScene.ts`, `SpriteRig.ts`, jump effects and tests; `tests/movement.test.ts`; and the new `e2e/tests/v7-move-live-gate.spec.ts`.
- Validation will be appended last and will include typecheck, Vitest, and the permanent live gate. I will not commit.

## Work log

- Recorded the work order and lease before implementation.

### Inventory and failing live baseline

- Enumerated the movement sentence across shared constants/math/schema, server input/authority/damage consultations, predictor replay/reconciliation, Arena input/effects, `SpriteRig`, focused tests, and the repository's private-stack Playwright idiom. The current slide is a ten-tick decaying, steerable carry with movement-speed admission, early release, Space-to-hop, airborne steer, prelanding buffer, landing chain, and six-running-tick rearm. Those semantics are the opposite of a one-shot fixed roll and explain the broad atomic migration required by the work order.
- The current distance jump is authored at 372 px / 620 px/s with a 380 px/s vertical impulse and validated landing, steering, pit, pound, and freeze seams. Its entry path is the failure: Space classifies a hold after 150 ms, enters `STANCE_CROUCH`, then waits another 500 ms authoritative commit. A tap otherwise emits the smaller ordinary hop on release.
- The repository history contains the pre-slide production roll treaty: stance wire id 4, frozen accepted direction, eight 20 Hz speed samples totaling exactly 188 px over 400 ms, five-tick contact/projectile/locked-melee opening, vulnerable moving tail, separate parry lock, and a three-second cooldown. I will restore that established deterministic sentence through the current append-only schema tail rather than introduce a second movement encoding.
- Added the permanent measured live gate first and ran it against unmodified behavior with `DD_V7_MOVE_EVIDENCE_PHASE=before` on a private ephemeral stack. Retained `before/live-capture.json`, `roll-natural-frame.png`, and `long-jump-natural-frame.png`; no owner listener was touched.
- Baseline roll measurements across east/west cardinal and northeast/southwest diagonal real Shift/Ctrl inputs were all approximately 238.07 px over 500 ms, not the 188 px / 400 ms fixed sentence. Rendered whole-card rotation span was 0 radians in every case, so the shipped pose is a low slide/fold rather than a tumble. The immediate repeat was accepted after movement rearm instead of rejected by a time cooldown. Maximum predictor/server separation reached 397.30 px in the first sample and approximately 148.68 px thereafter.
- Baseline one-frame Space evidence observed `STANCE_CROUCH`; authoritative distance-jump entry occurred about 1,538.5 ms after the input marker and predicted entry about 1,066.1 ms after it. The eventual legacy charged jump still traveled 371.99 px and peaked at 58.56 px, confirming the authored flight geometry itself is healthy. The attempted airborne Space follow-up did not enter pound because the delayed second flight had not begun.

### Shared sentence and client presentation

- Replaced the slide constants/math with the restored fixed-roll law: eight 50 ms samples (`680, 640, 580, 500, 420, 340, 300, 300` px/s), exactly 188 px over 400 ms, five opening ticks, a six-tick attack cancel, a ten-tick parry lock, and a three-second cooldown. Wire id 4 and the append-only `slidePhase` / `slidePhaseTick` schema tail remain in place as compatibility names; the retired crouch id and phase values remain tombstones rather than reordered fields.
- Reworked `GameRoom` and `SelfPredictor` around the same shared speed sampler. The accepted roll freezes its direction, has no run-up/release/hop/landing-chain/rearm states, resolves naturally to ordinary movement, and starts its time cooldown when it ends or is cancelled. Contact, projectile, and locked-melee opening checks continue to consult the existing shared five-tick predicate; broad AoE and parry-reward paths were not added.
- Grounded Space now buffers and launches the existing distance jump immediately when ready; airborne Space remains pound. The Space classifier no longer emits or waits in crouch/charge state. A Space press during the short roll tail buffers the long jump for the first legal server/predictor tick.
- Replaced the low slide pose with a whole-card roll driven by committed roll tick progress: one signed full revolution over the 400 ms motion, plus a bounded tuck-only reduced-motion variant. Added pure presentation-geometry tests. Removed the crouch charge pose, charge audio, second-coil timer, and charge-distance indicator; the HUD pip now represents the fixed three-second roll cooldown.

### Deterministic verification pass

- Converted the historical slide-hop authority/predictor assertions to the V7 sentence and removed the retired test blocks. Focused shared, server, predictor, and presentation tests now pass: 322/322.
- The focused pass exposed two real tick-order issues. Roll-tail Space buffering expired at the natural-recovery edge, so both authority and prediction now retain it through the next first-legal tick. Immediate long-jump launch initially added a thirteenth horizontal sample (403 px); launch height/pit immunity now begin on the input tick while only that tick's horizontal displacement is deferred, preserving the authored twelve samples and exact 372 px reach without restoring any charge delay.

### Permanent measured live gate

- Added `e2e/tests/v7-move-live-gate.spec.ts` using the repository-isolated `runArenaSpec` stack on ephemeral ports. It drives physical Shift, Ctrl, and a one-render-frame Space tap; records outgoing input, authority patches, predictor state, synced momentum, reconciliation error, and rendered root/body geometry; and writes durable JSON plus natural-frame screenshots under `docs/owner-notes-audit-v7-evidence/movement/after/`.
- The final passing capture measured east Shift, west Ctrl, northeast Shift, and southwest Ctrl at 188.000 px in 400 ms with direction dot 1.000000. The immediate repeat was rejected and later real-input directions were accepted after cooldown. Maximum sampled reconciliation error was 82.80 px, within the existing 200 px envelope.
- Three independently sampled directions projected the full 6.283185-radian whole-card revolution; the first direction's sparse two-frame sample projected π, while the permanent assertion requires at least one complete live-rendered revolution and the pure geometry suite proves the same 2π law at every tick/direction. Visual inspection of `roll-natural-frame.png` confirms the full player card is in the tucked roll pose rather than the retired low slide/fold.
- The physical Space tap was released on the next gameplay render (`heldRenderFrames: 1`). Its input was first acknowledged while authority was already in distance-jump flight, prediction onset was immediate, and no authority or prediction frame entered `STANCE_CROUCH`. The final unobstructed live sample traveled 388.000 px (inside the authored 330–405 px band), peaked at 58.555 px, and landed normally. A second physical launch plus a protocol-valid live airborne pound edge entered `STANCE_POUND` and landed normally; deterministic input coverage separately proves airborne Space emits that edge. Pit immunity/landing and freeze cancellation remain covered by the authority/predictor suites.
- Loaded-render probing exposed two input-delivery hazards outside the movement law: ordinary catch-up heartbeats could exhaust the per-tick flood budget immediately before a real traversal edge, and Phaser could coalesce the Space `JustUp` sample. Authority now reserves exactly one bounded, monotonic traversal-edge slot per tick, and the Space classifier re-arms from the stable released state. Focused tests cover both without widening the ordinary input or action flood budgets.
