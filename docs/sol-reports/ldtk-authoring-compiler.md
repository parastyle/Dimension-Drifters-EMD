# Sol 1 — LDtk authoring compiler

## Understanding and plan

I own only the new-file authoring surface assigned to `ldtk-authoring-compiler`: the canonical LDtk project and starter level, standalone deterministic importer, generated shared arena catalog, author guide, hostile fixtures, focused tests, and this report. I will not edit any leased central/runtime/weapon file, register package scripts or asset checks, wire runtime loading, touch the live stack, or implement deferred features.

The importer must treat LDtk as the sole authored-geometry source, accept only the pinned schema/version and fixed arena contract, resolve references by IID, canonicalize order independently of editor arrays, reuse the exported shared map validation/navigation laws and constants, and fail non-zero with level/layer-or-entity identity plus tile/world coordinates. It must never repair invalid geometry or fall back to procedural content. Generated TypeScript will be emitted only through `tools/artkit/lib/emit.mjs` in write/check modes.

Work plan:

1. Read the complete work order, reporting regime, and shared `mapgen.ts` contract in the mandated order; inspect only the narrow supporting surfaces needed afterward.
2. Record the exact authored LDtk schema and deterministic compilation rules, then build the canonical project and owner playground.
3. Implement the standalone importer with strict schema/version checks, stable sorting, IID reference resolution, shared-law validation, coordinate-rich diagnostics, and generator-owned output.
4. Add hostile fixtures for every required failure mode plus a source-order shuffle determinism test.
5. Run focused generation/check/test validation, append evidence as it is obtained, and finish with the required explicit verdict and Sol 2 handoffs.

## Progress log

- 2026-07-22: Created this report as the first repository write. No implementation or investigation preceded it.
- 2026-07-22: Read the complete orchestration plan, durable-reporting README, and all of `packages/shared/src/mapgen.ts` in the mandated order. Confirmed Sol 1 owns the exact new paths listed in the plan and must leave package registration/runtime integration to Sol 2.
- 2026-07-22: Shared-law finding: `validateArena` and `auditArenaNavigation` are exported, but construction of an authored `ArenaMap` currently requires private zone-seed derivation/topology helpers and a `PoiCollisionIndex`. Sol 1 will not duplicate private reachability, clearance, zone-topology, or repair logic. Structural LDtk validation belongs here; authoritative geometry red-to-green coverage remains a recorded Sol 2 handoff per the compiler gate.
- 2026-07-22: Confirmed the pre-existing dirty worktree is the concurrent V7 program described by the owner (weapon/catalog/SpriteRig/perf paths). I will preserve it untouched; my write set remains limited to the Sol 1 lease.
- 2026-07-22: Implemented the first importer/generator pass in `tools/levels/ldtk-import.mjs` and `tools/levels/gen-authored-arenas.mjs`. The compiler pins LDtk JSON 1.5.3, validates exact embedded definitions and fixed dimensions, canonicalizes by normalized level ID and row/column/IID entity order, resolves cluster refs by IID, derives row-major zone seeds, constructs the shared map shape, invokes both `validateArena` and `auditArenaNavigation`, hashes canonical output, and renders through the repository emitter. The CLI supports isolated `--source`/`--out` paths so drift behavior can be tested without touching canonical output.
- 2026-07-22: Created `data/arenas/dimension-drifters.ldtk` with embedded `owner-playground`: fixed 60×60/80px Terrain and Zones IntGrids, an asymmetric authored pit/zone motif, centered safe spawn, six cluster anchors, and 24 explicitly referenced S/M/L/XL landmarks. The file passes the official LDtk 1.5.3 JSON Schema with zero errors.
- 2026-07-22: Built `@dd/shared` without error and ran the standalone generator. The canonical starter passed both shared validators and generated `packages/shared/src/authored-arenas.generated.ts` with revision `sha256:329afb6027ca…` through `emit.mjs`.
- 2026-07-22: Added the author guide and ten declarative hostile mutations covering unsupported version, dimensions, grid size, illegal tile, border, spawn, stranded island, cluster count, dangling reference, and missing spawn. Added the focused importer suite, including no-mutation/no-repair assertions, a two-level definition/layer/entity/field reorder proof, isolated `--check` drift proof, and committed-generator ownership proof.
- 2026-07-22: Retained four additional Sol 2 hostile fixtures for laws the current shared API cannot prove with the required witness: outer spawn-disc/jitter clearance, disconnected-zone component coordinates, overlapping/undercut POI footprints, and representative two-gate capacity. They are marked `*.sol2-handoff.json` and are deliberately not “made green” with importer-local geometry.

## Handoffs for `ldtk-runtime-integration`

1. Register `node tools/levels/gen-authored-arenas.mjs` in the existing `pnpm gen`/`pnpm gen:check` chains and add the source/generated bijection to `tools/artkit/check-assets.mjs`. Those existing files are outside this Sol's lease.
2. Extend the shared authored-map construction/load seam in `packages/shared/src/mapgen.ts`, then wire server/client state and selection as the plan specifies. Sol 1 emits immutable catalog data only and does not alter runtime behavior.
3. Strengthen the one shared validation law rather than the importer. Current `validateArena` checks the fixed centre cell for terrain reachability instead of deriving the start from `map.spawnX/map.spawnY`; it does not prove the full `MAP_SPAWN_CLEAR_TILES` disc or co-op jitter envelope, `MAP_PIT_MAX`, the six-landmark cluster upper bound, landmark footprint plus `MAP_POI_GROUND_CLEARANCE`, radius-aware POI spacing/spawn clearance, or representative post-boss gate-pair capacity. The procedural generator enforces several of these only in private construction helpers. Export/centralize the authoritative checks in `mapgen.ts`, make `validateArena` enforce them, and keep the importer calling that shared API.
4. Add coordinate/IID witness data to shared validation results. `invalid/disconnected map-zone layout` and the sealed-cell navigation reason currently expose only prose/counts, so Sol 1 cannot identify the exact offending component/cell without duplicating private BFS/topology logic. The retained disconnected-zone fixture expects the new witness.
5. Run the four `*.sol2-handoff.json` fixtures red-to-green through the strengthened shared validator. In particular, `blocked-spawn.json` proves a blocked centre today, while `blocked-spawn-disc.sol2-handoff.json` preserves the missing outer-disc case at tile `(33,30)`.

## Validation

- Official format: `dimension-drifters.ldtk` validates against LDtk's official 1.5.3 `JSON_SCHEMA.json` with `0` errors. LDtk itself is not installed in this environment, so I could not launch the interactive editor; schema validation is the available source-format proof.
- Source census: 1 embedded 4800×4800 level; 60×60 cells at 80px; 167 pits (4.64%); zone areas Commons/Cover/Scar = 1547/1043/1010; one spawn; six clusters with four landmarks apiece; 24 total landmarks across S/M/L/XL.
- Generation: two consecutive write runs plus `node tools/levels/gen-authored-arenas.mjs --check` produced the same catalog SHA-256 `5631FA1F2F470FB52B705ED81899562C59DF4221860EB0D49CC69A52895B5AE8`; the authored record revision remained `sha256:329afb6027ca…`; check mode reports the committed catalog in sync.
- Focused tests: `pnpm exec vitest run tests/authored-arena-import.test.ts` passed 5/5 tests. The parameterized hostile case covers all 10 Sol 1 red fixtures, asserts source remains byte-equivalent after rejection, and verifies identity/tile/world diagnostics. The suite also proves repeat byte identity, two-level sort order, definition/layer/entity/field reorder immunity, generator ownership, and non-writing valid drift detection.
- Static checks: `pnpm --filter @dd/shared build` passed after generation; `pnpm typecheck` passed all shared/client/server workspaces; Biome passed the importer, generator, and test; all 21 owned files parse where applicable, end with a newline, and contain no trailing whitespace.
- Aggregate tests: `pnpm test` ran 1,753 tests and finished with 1,743 passing / 10 failing. All 10 failures are outside this lease in the concurrent V7 baseline: nine weapon codegen field-survival cases in `tests/data-consistency.test.ts` and one Headsman/VfxPlayer string assertion in `tests/v61-ship-fix.test.ts`. The focused LDtk suite passed within the aggregate. I did not edit leased V7 files or weaken those assertions.
- Lease audit: every Sol 1 path is new/untracked and exactly within the plan's exclusive list. The worktree's tracked weapon/SpriteRig/performance changes and other V7 untracked paths predated or progressed concurrently with this work and remain untouched. No package script, asset checker, mapgen/state/constants, server/client runtime, weapon catalog, listener, browser, or live stack was modified or invoked.

VERDICT: SHIPPED the canonical LDtk 1.5.3 project with owner-playground, standalone fail-loud deterministic compiler, emitter-owned shared catalog, author guide, 10 active hostile fixtures, 4 retained shared-law fixtures, and focused tests; DID NOT SHIP runtime loading, pnpm/assets registration, deep-link/state/server/client wiring, browser evidence, or live play; COULD NOT PROVE an interactive LDtk launch, live runtime behavior, the concurrent V7 aggregate, or the four private/missing shared geometry witnesses; SOL 2 HANDOFFS are (1) register gen/gen:check and the asset census, (2) add shared authored construction plus runtime/state/server/client wiring, (3) centralize full spawn/jitter, pit ceiling, cluster upper bound, POI clearance/spacing, and gate-capacity validation in mapgen, (4) return exact topology/navigation witness coordinates and IIDs, and (5) make all four `*.sol2-handoff.json` fixtures red-to-green through that shared law.
