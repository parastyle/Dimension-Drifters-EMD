# V6.1 SHIP+FIX live evidence

## Cursor-circle sweep

`cursor-circle-before-*` was captured from the live pre-fix recipe. The paired JSON records the
target-anchored `shockwave-ring` / `sigil-ring` layer and the visible Graphics ring. The three
families are Revenant (void), Voltfang (shock), and Sanctified Headsman (holy).

`cursor-circle-after-*` was captured after the systemic replacement. Each paired JSON records no
generic ring layer and a visible installed element-splat particle pack (`void-splat`, `shock-splat`,
or `holy-splat`).

The large cyan circle that spans the Testing Grounds screenshots is the map's persistent spawn-safe
boundary, centered on the spawn point. It is not an impact or cursor VFX. The probed impact is at the
aim point to the right of the player.

## Sanctified Headsman seam

`headsman-seam-after-mid-swing.png` and `headsman-seam-after.json` are a live, paused mid-swing
capture using the pistol-twirl sampling idiom. Three consecutive animation-frame samples cover the
swing arc. Every sample reports zero visible gap, at least 42.9 px of opaque join overlap, and the
extension at depth 2419 beneath the weapon at depth 2420.

`v61-live-after-summary.json` is the combined assertion report for all four probes.
