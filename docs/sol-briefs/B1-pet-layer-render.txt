You are Sol `pet-layer-render`, the RENDERING/DEPTH arm of a 2-Sol pet layered-texture design panel.
Slug: `pet-layer-render`. Write `docs/design/pet-layer-render.md`. DESIGN ONLY — no product edits.

## The owner's ask, verbatim
"put a couple Sols on figuring how to do layered texture pets. That is 1 image ontop of another image
representing a different depth. Basically get the pets to have the same bobbing feel the characters do
using a 2 layer trick."

## Your mandate: the rendering technique
Figure out HOW to render a pet as two stacked image layers at different depths so it gets the same
alive bobbing feel the characters' separated heads have. You own the RENDERING/animation side; the
sibling `pet-layer-pipeline` Sol owns art-production + part-slot integration.

- Study how the CHARACTER bobbing head already works: `docs/design/chars-3-headrig.md` and the real
  `SpriteRig.ts` head-spring (bounded float, counter-bob, phase vs footfall). The pet 2-layer trick
  should reuse this proven machinery, not reinvent it. Confirm what exists.
- Study the current pet rendering: `packages/shared/src/pets.ts`, the pet part-slot manifest
  (`packages/client/public/sprites/pets/pet-parts-manifest.json`), and how pet sprites are drawn today.
- Define the 2-LAYER MODEL for pets: which layer is "back" (further depth) and which is "front", how the
  front layer bobs/parallaxes against the back to read as depth + life, the amplitude/frequency/phase
  (reuse the character bob values or adapt), and how depth-sorting keeps the layers correctly ordered
  relative to the pet, the player, and the world. Cover the top-down-ish camera read.
- Address the general case: a pet may want the 2 layers to be body(back)+overlay(front) — e.g. a shell
  back + creature front, wings behind + body front, a glow behind + body front. Give 2-3 concrete pet
  examples showing which layer is which and how the bob sells depth.
- Note interactions the implementation must not break: existing pet band/evolution/fusion part slots,
  performance with many pets on screen, and reduced-motion behavior.

Deliver a concrete rendering spec an implementation Sol could build, explicitly reusing the character
head-spring. Say what is genuinely NEW vs reused. Name the exact handoffs to `pet-layer-pipeline`.

## Rules
- Design only. Your ONLY write is your report. No product code/assets/tests.
- Live game on 5180/2567 is UP — do not touch. Other Sols are in this worktree — stay in your lease.
- FIRST action: create your report with understanding + plan (durable-report regime,
  `docs/sol-reports/README.md`). Append as you go. Record assumptions; do not ask.
