You are Sol `char-art-kuro-oni`. You generate the art for EXACTLY ONE subject: Kuro-Oni, the Demon Mask.
C:/Users/Exped/DDv2, branch feat/v0.118-metagame.

## ART-GENERATION LAW (binding)
ONE subject per agent. Generate Kuro-Oni, the Demon Mask and NOTHING else. See docs/design/OWNER-DECISIONS-characters-pets.md.

## Inherit the locked reference (coherence without bleed)
The Drifter is the anchor. READ `data/characters-v2.json` — obey its `houseStylePreamble`
(DRIFTER_STYLE_LOCK_V1), `canonicalBody` (ALL SAME SIZE), `palette` band, proportions, and head
anchor EXACTLY. Study the reference art at `packages/client/public/sprites/chars-v2/drifter/body.png`
and `head.png` so your character reads as the same cast, same scale, same line weight, same shadow
grammar — but with ITS OWN identity below.

## This character
Kuro-Oni, the Demon Mask (legacy id cc-kuro-oni-the-demon-mask). Face concealment (HARD, no visible biological face ever):
a huge horned CRIMSON ONI DEMON MASK with brass fangs as the head; every opening is black or flat glow; top-heavy lamellar-armored bruiser body.

## Deliverable (ONE subject)
Generate via codex image generation as TWO separate transparent layers, SAME body scale as the Drifter:
- `packages/client/public/sprites/chars-v2/kuro-oni/body.png` — body only, NO head, no neck stub, closed shoulder roof.
- `packages/client/public/sprites/chars-v2/kuro-oni/head.png` — the bobbing head only (the concealment above),
  sized/anchored to the SAME head-anchor/overlap as the Drifter reference so the rig bobs it identically.
Append this character's record (id, name, concealment, head anchor if it deviates, notes) to its OWN new file `data/characters-v2.d/kuro-oni.json` (NEVER touch the shared data/characters-v2.json — that races with sibling agents; the orchestrator merges).
Write `docs/design/char-art-kuro-oni-evidence.md`: face-law checklist PASS, head separable, no neck,
on-style vs the Drifter lock, same size.

## HARD RULES
- Generate ONLY Kuro-Oni, the Demon Mask. No other subject. A visible face = automatic reject + regenerate.
- Do NOT wire into the game (no SpriteRig/manifest/catalog). Writes limited to
  `packages/client/public/sprites/chars-v2/kuro-oni/**`, `data/characters-v2.d/kuro-oni.json`,
  `docs/design/char-art-kuro-oni-evidence.md`, and your report. Other char agents write their OWN dirs in
  parallel — touch ONLY your kuro-oni paths and your own JSON key to avoid clobbering siblings.
- Live game on 5180/2567 is UP — do not touch. LF endings only.

## Reporting
FIRST action: write `docs/sol-reports/char-art-kuro-oni.md` (understanding + plan). Append as you go.
Final line: verdict — Kuro-Oni, the Demon Mask body+head shipped, face-law checklist result, on-style confirmation vs the
Drifter lock. Work autonomously; record assumptions; do not ask.
