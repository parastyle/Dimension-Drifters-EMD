You are Sol `pet-layer-pipeline`, the ART-PRODUCTION/INTEGRATION arm of a 2-Sol pet layered-texture
design panel. Slug: `pet-layer-pipeline`. Write `docs/design/pet-layer-pipeline.md`. DESIGN ONLY.

## The owner's ask, verbatim
"put a couple Sols on figuring how to do layered texture pets. That is 1 image ontop of another image
representing a different depth. Basically get the pets to have the same bobbing feel the characters do
using a 2 layer trick."

## Your mandate: how the 2-layer pet art is PRODUCED and slotted
The sibling `pet-layer-render` Sol owns the rendering/depth animation. You own how the two layers are
GENERATED and how they plug into the existing pet part-slot + evolution + fusion system.

- Verify the pet system: `packages/shared/src/pets.ts`, `pet-parts-manifest.json`, the 3 bands
  (Hatchling/Awakened/Ascendant), and the existing part slots. The code says slot manifests "replace
  parts without migrating accounts" — the 2-layer approach must respect that.
- Reconcile the 2-layer trick WITH the pet panel already delivered: `docs/design/pet-4-art-pipeline.md`
  (generation + slice), `docs/design/pet-2-evolution.md` (form slots), `docs/design/pet-3-fusion.md`
  (part recombination). The 2 depth layers are NOT a competing system — say how they RELATE to the
  part slots. Is "back layer / front layer" just two of the existing slots tagged with depth? Or a new
  depth attribute on every slot? Recommend the simplest model.
- Define how the ART is generated as two separable depth layers per pet/stage: the prompt/pipeline
  changes on top of pet-4 (a back-layer render + a front-layer render with documented overlap and a
  hidden seam), and the review checklist addition (the two layers must compose cleanly and bob without
  revealing a gap).
- Cover evolution + fusion: when a pet evolves or fuses, how do the two depth layers update? Does a
  fused pet inherit back-layer from one parent and front from another? Keep it consistent with pet-3.
- Data/systems: what does a pet definition need to declare its 2 layers (which slots are back vs front,
  the depth offset)? Name the files a future implementation touches and the census/manifest impact.

Deliver: the depth-layer data model, the art-production additions to the pet-4 pipeline, the
evolution/fusion interaction, and the implementation touch-list. Reference the character bobbing-head
approach as precedent. Name the handoffs to/from `pet-layer-render`.

## Rules
- Design only. Your ONLY write is your report. No product code/assets/tests.
- Live game on 5180/2567 is UP — do not touch. Other Sols are in this worktree — stay in your lease.
- FIRST action: create your report with understanding + plan (durable-report regime). Append as you go.
  Record assumptions; do not ask.
